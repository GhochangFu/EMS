# F2.8 — Replace hardcoded PUE SQL with user-defined derived tags

*Backlog row `docs/BACKLOG.md:449` · P1 · Effort 2 (re-set below) · Wave 2 · Depends `F2.4`, `F2.9` (both ✅) · Gate: the owner's five rulings of 2026-09-05, **no ADR** · Branch `feat/F2.8-pue-derived-tags` off `main` at `58d7019`*

*Planned 2026-09-05 by `plan-architect` (Fable) from the owner's five rulings of 2026-09-05 (step 2). The rulings are the gate record; §2 quotes them and the PR body must repeat them.*

---

## 1. Goal, and what is out of scope

**Goal.** PUE stops being a curve fitted to one number and becomes a ratio of two `bms-calc-v2` aggregates the engine computes on a site's incomer. The stock incomer entry (`electrical-feeder`) gains three derived points — `site_kw`, `it_kw`, `pue` — so any tenant that imports it gets the ratio by configuration; the ESKOM demo seed pins each site's `incoming-supply` asset to a template carrying the same three points so the running stack shows a measured number; and the API reads Σ `site_kw` / Σ `it_kw` over the incomers in scope, returning `null` where nothing is configured. All three `estimatePue()` copies and every `1` sentinel are deleted, the two contracts go nullable, and the three pages render a dash with a "not configured" hint.

**Explicitly out of scope.**

- **Authoring affordances** for `v2` (dialect selector, coverage input, `@` completion) — `F2.22`.
- **A site-level role, or PUE on every asset.** Ruling 1: the point lives on the incomer only.
- **Any change to `apps/sim`'s magnitudes** in this row's tasks — open question 1 puts that fork to the owner before Task 3; the plan is written for either answer.
- **Renaming the seeded `it-rack` group.** `IT_LOAD` is an additional group per site (ruling 2's reserved code); `it-rack` stays because `apps/web/src/lib/control-room-access.ts:40` and migration `0013` name it.
- ~~**A freshness bound on the KPI read.**~~ **Reversed by the owner on 2026-09-06** (§11 decision 6). The bullet argued that the PUE read should be unbounded for parity with `totalKw` because "the ribbon's existing `Stale` badge covers the page". Review measured that false: `use-executive-dashboard.ts:68-71` derives `stale` estate-wide from the last Socket.IO `kw` tick, so one silent site among nine cannot move it, and `energy-page.tsx` and `reports-panel.tsx` pass no `stale` prop at all. `latestPueRatio` now ignores rows older than `PUE_LATEST_MAX_AGE_SECONDS` (900 s); `windowedPueRatio` is unchanged, because its caller names the window.
- **The CSV row label `PUE estimate`.** Unchanged, because `reports.serialise.spec.ts`'s `UNCHANGED_OUTPUT` is ADR 0026's byte-level blast-radius proof and a label edit would force a re-pin for no product gain. The UI labels drop "(est.)".
- **No `AGENTS.md` edit, no `BACKLOG` ✅, no roadmap mirror on this branch** — §9.10, separate `chore(agents):` PR.

---

## 2. Gate record — the owner's five rulings (2026-09-05, step 2)

Quoted so a later reader finds the scope where it was set. **No ADR is owed**: no new dependency, no schema change, no new module, nothing promoted out of §6.

1. **Owner of the PUE point: the site's `incoming-supply` asset.** The electrical stock entry's incomer gains three `bms-calc-v2` derived points: `site_kw = sum({kw} @site)`, `it_kw = sum({kw} @group('IT_LOAD'))`, `pue = {site_kw} / {it_kw}`. Not a new site-level role, not a point on every asset.
2. **IT load = an asset group with the reserved code `IT_LOAD`.** `bms.asset_groups` is unique on `(location_id, code)` (migration `0010`), so the group is per site, and `@group('…')` resolves against the owner's location (`calc-scope.service.ts` header, ADR 0055 decision 9). A tenant that prefers another code edits the formula.
3. **Estate figure = kW-weighted:** Σ `site_kw` / Σ `it_kw` over the incomers in scope; one site in scope reduces to that site's own ratio; sites with no PUE point are left out of both sums.
4. **Nothing configured → `null`.** `pueEstimate` becomes nullable in both contracts (`packages/shared/src/contracts/dashboard.ts:126`, `operations.ts:20`, ADR 0030); the three pages (`dashboard-page.tsx:83`, `energy-page.tsx:130`, `reports-panel.tsx:224`) show a dash with a "not configured" hint; `reports.serialise.ts:89` writes a dash. The fitted curve `1.22 + min(0.45, totalKw / 12000)` is **deleted** from all three copies — `dashboard.service.ts:541`, `reports.service.ts:332`, `apps/web/src/lib/pue-estimate.ts:2` — no fallback, no `1.0` sentinel (the empty branches at `dashboard.service.ts:436,476,667` and `reports.service.ts:146` return `1` today).
5. **No ADR.** No new dependency, no schema change, no new module, nothing promoted out of §6. The plan quotes these rulings as its gate record; the closure row repeats them.

---

## 3. Ground truth this plan is built on (measured 2026-09-05, live compose database, `docker exec bms-postgres-1 psql`)

| Fact | Where measured |
|---|---|
| **The stock incomer is `electrical-feeder`** — "Feeder / incomer — multifunction energy meter", `assetType: "feeder"`, 33 measured points, **zero derived**, `stockVersion: 1`. Its kW key is **`kw`** (sortOrder 11, tier C). `demand_kw` (sortOrder 20) is an optional X-tier point, not the load. | `stock-catalog/electrical-feeder.ts:108-239` |
| The stock formula therefore uses `{kw}` exactly as ruling 1 spells it. | derived |
| **An override cannot add a point the pinned template does not declare.** `resolveWritableContext` reads "the pinned version's declaration of this point"; `calc-definitions.service.ts:105-159` builds definitions from `template_points WHERE kind = 'derived'` INNER JOINed on `assets.template_id`, with `asset_points` only LEFT-joined as a coalesce. **So the demo cannot use the override; it needs a template that declares the three points, pinned to the incomer only.** | `asset-point-calc-override.service.ts:401-414`, `calc-definitions.service.ts:105-159` |
| The override *does* accept `bms-calc-v2` (`calcDialectSchema.nullable()`) and the cycle check runs on the merged dialect — irrelevant here, recorded so nobody re-measures it. | `asset-point-calc-override.schema.ts:56`, `.service.ts:212-235` |
| ESKOM's 148 demo assets are pinned to four seeded baselines: Electrical Baseline 50, Environment 22, HVAC 14, IT 14. **Electrical Baseline declares `backup_min, battery_temp_c, breaker_main, current_a, kw, pf, voltage_l1_v`; IT Baseline declares only `pdu_util_pct`.** | `bms.template_points` |
| The 26 other ESKOM `asset_templates` rows are stock imports (`stock_code` set) with **no asset pinned** — created by hand through the UI/tests, not by the seed. `electrical-feeder` is **not** among them. | `bms.asset_templates` |
| **The simulator emits `kw` for electrical (50 assets), `cooling_kw` for HVAC (14), `rack_kw` for IT (14), and no `kw` on any IT asset.** ESKOM's last reading is `2026-09-04 15:57 UTC` — **the sim is not running now** (`docker ps` shows no `bms-sim-1`). | `telemetry.point_values`, `apps/sim/src/index.js:165,239` |
| **IT assets have no `asset_points` row for `rack_kw`** (only `pdu_util_pct`, `unmapped`, `SIM_PDU_UTIL_PCT`). Membership requires the key declared "by template or by an active mapping" (`readScopeMembers`, `calc-scope.service.ts:182-192`), so `sum({rack_kw} @group('IT_LOAD'))` resolves **zero members → `no_members`** until the seed writes those rows. | `bms.asset_points`, `calc-scope.service.ts:148-205` |
| Electrical `asset_points` rows are `unmapped` / `SIM_KW` / `rtu_id NULL` — written by `seedRuledPointCatalog` from rules. That is the shape the `rack_kw` rows must copy (`asset_points_source_ref_check`: `unmapped` ⇒ `rtu_id IS NULL`). | `ruled-point-catalog-seed.ts:77-91`, `pg_constraint` |
| **The incomers:** nine `*-CR-UTILITY*` assets (one per RSMOC site, including `CR-UTILITY-11KV` at Western Cape) already carry `role = 'incoming-supply'` on their electrical-group membership via `demoRoleForAsset`. **CSMOC Gauteng has `SWG-MDB1` (`lt-panel`) and `TX-L1-MV` (`transformer`), no incomer and no IT asset → no PUE point, left out of both sums by ruling 3.** "Decommissioned Substation" holds one electrical asset with no role. | `bms.asset_group_members`, `asset-groups-seed.ts:99-140` |
| IT assets per site: one at each of eight RSMOC sites, six at Western Cape, zero at CSMOC → `IT_LOAD` = 9 groups, 14 members. | `bms.assets` |
| **Measured demo ratio (latest per asset, last 7 d):** Western Cape 41.1 kW electrical / 12.6 kW rack = **3.26**; each other RSMOC site 92–262 kW / 0.3–3.0 kW = **40–300**; estate Σ 1447.3 / Σ 25.1 = **57.7**. The sim gives every RSMOC electrical asset `kw: 80 + rand·400` and every IT asset `rackKw` 1.42 or 2.81 — IT is ~2 % of electrical. **Open question 1.** | `telemetry.point_values`, `apps/sim/src/index.js:103,165` |
| `HEALTH_TEMPLATE_POINTS_SQL` declares on each `BASELINE-*` **every** `bms.asset_points` key of that domain's assets, with no `source_kind` filter. `CalcWriteService` creates `source_kind = 'computed'` rows for `site_kw`/`it_kw`/`pue` on first write. **So the first `compose up` after the engine runs would re-declare the three outputs as MEASURED points on `BASELINE-ELECTRICAL`.** Task 3 closes it. | `asset-template-health-seed.ts:180-204`, `calc-write.service.ts:146-157` |
| `verifyHierarchySeed` runs on every `db:seed` and therefore every `compose up` (the `migrate` service); the `api` service waits on it. Its ESKOM pass counts nothing this row touches; PHE counts are exact and unaffected. | `verify-hierarchy-seed.ts`, `docker-compose.yml:95` |
| `bms.point_keys` holds **604** rows; `template_points.point_key` and `asset_points.point_key` are FKs to it. The three new codes must be in a `*_POINT_KEYS` array or the seed's template insert and the stock import (`assertPointKeysActive`) both fail. | `pg_constraint`, `stock-catalog.ts:127-133` |
| `resolveAggregate`: zero declared members → `no_members`; `minCoverageRatio === null` → fail closed on the first non-fresh member; `fresh === 0` → `coverage_below_floor`. Division by zero in `evaluate` → `non_finite`, nothing written. | `calc-aggregate.ts:52-90`, `calc-scheduler.service.ts:254-259` |
| `derived()`'s `calcTrigger: "scheduled"` overload is the only way to author a `v2` stock point; **no stock entry uses it yet — its docblock names `F2.8`'s PUE entry as the first caller.** | `point-fields.ts:111-135` |
| `stock-catalog.spec.ts`'s derived branch asserts **every** stock derived point is `CALC_DIALECT` (v1), `streaming`, interval `null`, and validates the formula with the v1 `validateFormula` against measured keys only. `stock-catalog-deferrals.spec.ts:637-645` asserts **the feeder authors zero derived points**. `tests/adr-0055-calc-v2-invariants.test.ts:80,130-156` scans every `derived("…")` literal and requires it to parse under v1 with no `crossRefs`. **All three go red on the first v2 stock point, by design; each gains a v2 arm.** | those files |
| `DashboardService` and `ReportsService` both inject `FLEET_POOL` (BYPASSRLS) and thread the caller's `assetIds` as the containment (ADR 0043 Amendments 2/3, stated in `reports.service.ts:31-38`). The reader takes the caller's pool and the same `$n::uuid[]` scope; it joins no policied table. | `dashboard.service.ts:28`, `reports.service.ts:38` |
| `KpiTile` renders `—` for `status === "empty"` and always renders `hint`; `value` is `string \| null`. No page or panel touched here has a jsdom spec today. | `kpi-tile.tsx`, `ls apps/web/src/pages` |
| `ReportCell = string \| number`; strings pass the ADR 0026 formula-leader guard (`= + - @`). U+2014 is not a leader. | `reports.serialise.ts:69-84` |
| Line caps (§4.5): `point-keys-seed.ts` 951, `dashboard.service.ts` 940, `constants.ts` 936, `stock-catalog.spec.ts` 903 before the row. **Measured after the build (2026-09-05, as the hook counts — `split(/\r?\n/).length`): `stock-catalog.spec.ts` exactly 1000 (passes with zero headroom; the next assertion there goes to `electrical-classes.spec.ts`, which already imports `checkEntry`), `dashboard.service.ts` 957, `point-keys-seed.ts` 954.** The 903 prediction missed the v2 `checkEntry` arm. | `wc -l`, `.githooks/pre-commit.mjs:194` |

### 3.1 The demo formula differs from the stock formula, and where

| Point | Stock (`electrical-feeder` v2) | Demo (`BASELINE-ELECTRICAL-INCOMER`) | Why |
|---|---|---|---|
| `site_kw` | `sum({kw} @site)` | same | electrical assets emit `kw` |
| `it_kw` | `sum({kw} @group('IT_LOAD'))` | **`sum({rack_kw} @group('IT_LOAD'))`** | the sim's IT assets emit `rack_kw`, never `kw`; a stock incomer meters real IT feeders in `kw` |
| `pue` | `{site_kw} / {it_kw}` | same | — |

Both are `bms-calc-v2`, `scheduled`, `calcIntervalSeconds: 60`, `minCoverageRatio: null`, `maxInputAgeSeconds: null` (→ 300 s default). `pue` reads two derived siblings on its own asset, which `v2` admits (ADR 0055 decision 7) and the sweep resolves in the same tick through the `computedThisTick` overlay at the bucketed time (`calc-scheduler.service.ts:67-81`); 300 s > 60 s, so plan correction 60's stale-input trap does not fire.

**`minCoverageRatio: null` is a plan decision, not an owner question.** It is ADR 0055 decision 11's default (fail closed), and the ADR says relaxation is opt-in and visible. A tenant relaxes it on the imported draft; the demo runs on a simulator that keeps every member fresh.

---

## 4. Serial or parallel

**Serial, one branch, no worktree fan-out — recommended.** `F2.8` is not a ⭐ enabler. Two file-disjoint lanes do exist — Lane A: Tasks 1→2→3 (`packages/shared/constants.ts`, `packages/db/*`, `stock-catalog/*`, `tests/adr-0055`, `tests/f2.13`) and Lane B: Tasks 4→5 (`contracts/*`, `apps/api/{dashboard,reports,telemetry}`, `apps/web/*`) — and Lane B could run in a worktree. It is not worth it: each lane is about two unit-days, a second implementer re-derives §3 cold, and step 6 cannot begin until both lanes are on one branch anyway. Build in the order below; the lanes are named so the operator can split them if the calendar demands it.

**Model per unit** (operating model §2 ladder), stated in each task: Task 1 Sonnet · Task 2 Opus · Task 3 **Fable** · Task 4 Opus · Task 5 Opus · Task 6 Sonnet · Task 7 Sonnet for the evidence, `browser-verifier` for the browser, session model for the reading.

---

## 5. Tasks, in build order — test first in each

Build order is `shared → db → api → web → tests/`. Every new `*.spec.ts(x)` gets its name-sibling `*.test.ts(x)` wrapper (`tests/repo-invariants.test.ts` fails on an orphan), every new `tests/*.test.ts` is hand-added to the root `typecheck:tests` script, and every named mutation is run red before the commit. Gate command for every task unless stated: `pnpm build && pnpm typecheck:tests && pnpm test` from the **repo root** with all four role URLs set (a filtered runner sets a different cwd from CI's and skips the integration suites silently).

### Task 1 — The three point keys join the electrical vocabulary — **Sonnet**

**Test first.** Move the anti-vacuity floors to the new actuals, and confirm each is red before the array grows:

- `tests/f2.13-asset-stock-catalog-vocabulary.test.ts`: `pointKeys.length >= 897` → `>= 900` (three new `pointKey:` occurrences in `electrical-feeder.ts`); distinct keys `>= 592` → `>= 595`; `vocabulary.size >= 602` → `>= 605`. Update the comment lines that derive those numbers.
- `tests/f3.38-stock-catalog-vocabulary.test.ts:373` `vocabulary.size >= 602` → `>= 605`.
- `tests/f3.39-global-point-key-vocabulary.test.ts:662` `units.size >= 602` → `>= 605`; `:826` `codes.size >= 602` → `>= 605`. The `UNIT_BY_KEY` completeness case (`F3.41` Task 1 item 4) is what turns a forgotten unit into a red run rather than a NULL seeded on every boot.

**Then implement.**

- `packages/shared/src/constants.ts` — `ELECTRICAL_CLASS_POINT_KEYS` (line 304) gains, at the end of the §1 feeder block, `"site_kw"`, `"it_kw"`, `"pue"`, each with a trailing comment `// F2.8: derived, formula in electrical-feeder.ts` (the file's existing convention at line 443). Update the docblock's count sentence (`139 → 145` by `F2.12`, now `→ 148` by `F2.8`), and say why they are here: `assertPointKeysActive` checks derived keys too (the docblock at 296-303 already gives the rule).
- `packages/db/src/point-keys-seed.ts` — `UNIT_BY_KEY` gains `site_kw: "kW"`, `it_kw: "kW"`, `pue: ""` under the §1 feeder comment (`""`, not `null`, the way `pf` is spelled — a dimensionless ratio). No `GLOBAL_CATALOG` edit: `keysForDomain(ELECTRICAL_CLASS_POINT_KEYS, "electrical")` at line 803 already carries the array. Watch the §4.5 cap: 951 → 954.

**Gate**: the three floors were red, now green; `bms.point_keys` will read **607** after the next seed. **Commit**: `feat(shared): site_kw, it_kw and pue join the electrical class vocabulary`.

### Task 2 — `electrical-feeder` v2 carries the three derived points — **Opus**

Opus rather than Sonnet because the `checkEntry` v2 arm this task writes is the rule every later `v2` stock point is checked against, and the `adr-0055` scanner change decides which literals the superset property still covers.

**Tests first**, all red on the current catalog or on the current entry:

1. `apps/api/src/admin/asset-templates/stock-catalog/stock-catalog.spec.ts`
   - **`checkEntry`'s derived branch (lines 397-435) forks on dialect.** Import `CALC_DIALECT_V2`, `CALC_DIALECTS`, `MIN_CALC_INTERVAL_SECONDS`, `MAX_CALC_INTERVAL_SECONDS` from `@bms/shared`. Assert `CALC_DIALECTS.includes(point.formulaDialect)`. **v1 arm — byte-identical to today** (streaming, interval null, `validateFormula` against measured keys; ADR 0055 decision 3 keeps `v1`'s refusals). **v2 arm**: `calcTrigger === "scheduled"`, `Number.isInteger(calcIntervalSeconds)` within `[MIN, MAX]`, `minCoverageRatio === null || (0 < r && r <= 1)`; `parseFormula(formula, { dialect: CALC_DIALECT_V2 }).ok`; every **local** ref ⊆ the entry's declared keys (measured ∪ derived — decision 7 admits a derived sibling); every aggregate/qualified `pointKey` ⊆ the entry's declared keys as well (deliberately narrow — widen when a stock aggregate first needs a key another class carries, and say so in the comment). The failure message must name the dialect the formula was parsed under.
   - **The feeder block (lines 846-875)**: `points.length === 33` → `36`; sortOrder `0…35` (the loop already checks index order); `feederKeys.size === 36`; keep `required.length === 17` and change the optional count to **measured-only** optional `=== 16` (`point.kind === "measured" && !point.required`), and add `derived.length === 3` with the exact key set `["site_kw", "it_kw", "pue"]` in that order and each `formulaDialect === CALC_DIALECT_V2`. Rewrite the comment: the three are **not** from `docs/electrical-derived-taglist-v1.md` §1 — they are ruling 1 of `F2.8`'s gate, the ADR 0055 decision 6 example made concrete.
2. `stock-catalog-deferrals.spec.ts:635-645` — replace the "no derived point" guard: the feeder authors **exactly** `site_kw`, `it_kw`, `pue` and none of its six deferred codes (the per-entry loop above it already proves the second half; keep `content.kpis` absent). `DEFERRED_DERIVED_CODES["electrical-feeder"]` is unchanged — `losses_pct` is still a Σ-of-feeders the entry does not author. Rewrite the docblock at 520-528: the feeder's guard is no longer "a claim about the whole entry".
3. `tests/adr-0055-calc-v2-invariants.test.ts` part (b) — **the scanner must tell a v2 call from a v1 one, or it goes red on Task 2 and stays red.** Widen `DERIVED_RE` to capture the literal *and* the call's option object (`derived\(\s*"((?:[^"\\]|\\.)*)"\s*(?:,\s*(\{[\s\S]*?\}))?\s*\)`); a literal is v2 when its options contain `formulaDialect:\s*CALC_DIALECT_V2`. Keep every existing assertion over the v1 set. Add part **(f)**: at least **3** v2 literals were found (anti-vacuity — the count is 3 and all in `electrical-feeder.ts`), each parses under v2, each options object contains `calcTrigger: "scheduled"`, and each either fails v1 or carries a non-empty `crossRefs` (the `pue` literal parses under both, which is fine and expected — say so). Update the docblock counts (36 → 39 `derived(` calls, 7 KPI literals). **Run the mutation**: drop `formulaDialect: CALC_DIALECT_V2` from one call in a scratch copy and confirm (f) fails.
4. `tests/f2.13` — no change beyond Task 1's floors; its `pointKey:` scanner picks up the three new occurrences by itself.

**Then implement.** `stock-catalog/electrical-feeder.ts`:

- Three rows appended after sortOrder 32, in the shape of `electrical-transformer.ts:410-414`: `{ ...derived("sum({kw} @site)", { calcTrigger: "scheduled", calcIntervalSeconds: 60, formulaDialect: CALC_DIALECT_V2 }), pointKey: "site_kw", label: "Site load (Σ kW at this site)", unit: "kW", required: false, sortOrder: 33 }`; `it_kw` = `sum({kw} @group('IT_LOAD'))`, label "IT load (Σ kW, group IT_LOAD)", `unit: "kW"`, sortOrder 34; `pue` = `{site_kw} / {it_kw}`, label "PUE", `unit: null`, sortOrder 35. Import `CALC_DIALECT_V2` from `@bms/shared` (never a string literal — `adr-0055` part (c)). Import `derived` from `./point-fields`. No `meta` on a derived point (the iff rule at `stock-catalog.spec.ts:380-385`).
- `stockVersion: 1` → **`2`**, and a **VERSION HISTORY** entry beside the v1 line (ADR 0052 decision 6): v2 (2026-09-05, `F2.8`): three `bms-calc-v2` derived points per the owner's ruling; `IT_LOAD` is a reserved group code the importing organization creates per site; `minCoverageRatio` null = fail closed, relax on the imported draft; the value is at most one 60 s tick old (ADR 0055 decision 10's cost). Rewrite "Zero `kind: "derived"` points" at line 52 and the `losses_pct` bullet (the grammar *can* now express a Σ; it stays deferred because the tag list's definition needs the feeder set, which is `F2.22`-era content).
- `point-fields.ts:111-117` docblock: "No stock entry uses this overload yet — `F2.8`'s PUE entry is its first caller" → "First caller: `electrical-feeder` v2 (`F2.8`)".

**Gate**: `stock-catalog.spec.ts` parses the entry with `createAssetTemplateBodySchema` (line 290-323) — that is the import path's `superRefine` (`v2` ⇒ `scheduled` ⇒ interval present) run at build time. **Commit**: `feat(api): electrical-feeder v2 — PUE as three bms-calc-v2 derived points on the incomer`.

### Task 3 — The demo seed: `IT_LOAD` groups, `rack_kw` declared, one incomer template per site, and the re-seed that cannot corrupt a baseline — **Fable**

Fable because this unit spans four seed modules and the boot gate in one pass, writes under `FORCE ROW LEVEL SECURITY` as `bms_owner` (an RLS surface where a declined write is silent — `asset-template-health-seed.ts:231-247`), and defines the demo data every later step reads.

**Tests first.**

1. `packages/db/src/asset-groups-seed.spec.ts` — a new exported pure `demoGroupCodesForAsset(code, domain): readonly string[]` replaces the inline ternary at `asset-groups-seed.ts:169-178`: `("X", "it")` → `["it-rack", "IT_LOAD"]`; `hvac` → `["hvac"]`; `environment` → `["environment"]`; electrical `CR-UPS-1` → `["ups-battery"]`, `CR-Q1` → `["electrical"]`; anti-vacuity: none of the 48 PHE codes (`loadPheCatalog`, already imported there) yields `IT_LOAD`. The group name map gains `IT_LOAD → "IT load (PUE)"`.
2. `packages/db/src/asset-template-health-seed.spec.ts` `assertTheDeclaredPointsAreMeasuredAndNotDerived` — add `expect(HEALTH_TEMPLATE_POINTS_SQL).toContain("ap.source_kind <> 'computed'")`, with the reason in the docblock: `CalcWriteService` creates `computed` rows for `site_kw`/`it_kw`/`pue` on the incomer, and without the predicate the next `compose up` declares the engine's outputs as MEASURED on `BASELINE-ELECTRICAL`.
3. **New pair** `packages/db/src/pue-demo-seed.spec.ts` + `.test.ts` (the `asset-template-health-seed` precedent; `vitest.config.ts` lists `packages/db`, `typecheck:tests` runs its tsconfig — nothing to wire): the three exported demo formulas parse under `CALC_DIALECT_V2` (`parseFormula` from `@bms/shared` — a runtime dependency of `packages/db` already, see `point-keys-seed.ts:8`) with `crossRefs` keyed `sum(kw)@site`, `sum(rack_kw)@group:IT_LOAD`, and `refs` `["site_kw","it_kw"]` for `pue`; the template SQL contains `ON CONFLICT (organization_id, code, version) DO NOTHING`; the pin SQL's `WHERE` names the `BASELINE-ELECTRICAL` template so an operator-migrated pin is never touched; the points SQL passes the dialect as a **parameter** and contains `'derived'`; the `rack_kw` SQL contains `'unmapped'` and `NULL` for `rtu_id` (the `source_ref_check`).
4. `packages/db/src/verify-hierarchy-seed.ts` ESKOM pass — three counts, fixed seed cardinalities read off the ESKOM catalog (a repository file), **not lifetime counters**: incomers pinned to `BASELINE-ELECTRICAL-INCOMER` = **9**; `IT_LOAD` memberships = **14**; `asset_points` rows `(domain it, point_key rack_kw)` = **14**. This is the boot gate (`compose up` → `migrate` → `api` waits), and CI's "Validate seed against a fresh schema" runs it once against a cold database, which is the only place the seed *order* is proved.

**Then implement.**

- `asset-groups-seed.ts` — the loop iterates `demoGroupCodesForAsset(...)`; the `IT_LOAD` group's description names ruling 2 and `@group('IT_LOAD')`. `COALESCE(role)` upsert unchanged. PHEWB has no `it` asset → no `IT_LOAD` at PHE, by construction.
- `asset-template-health-seed.ts` — `HEALTH_TEMPLATE_POINTS_SQL` gains `AND ap.source_kind <> 'computed'`; docblock records why ("Four templates" → still four *domain* baselines; the incomer template is `pue-demo-seed.ts`'s).
- **New** `packages/db/src/pue-demo-seed.ts`, `seedPueDemo(pool, organizationId)`, run inside the ESKOM bracket **after** `seedAssetTemplateHealth` (`seed.ts:240`):
  1. `PUE_DEMO_RACK_KW_POINTS_SQL` — one `asset_points` row per active `it`-domain asset: `(organization_id, asset_id, 'rack_kw', 'SIM_RACK_KW', NULL, 'unmapped', pk.unit, true)` joined to `bms.point_keys` for the unit, `ON CONFLICT (asset_id, point_key) DO NOTHING`. Membership needs the mapping; `HEALTH_TEMPLATE_POINTS_SQL` then declares `rack_kw` on `IT Baseline` at the *next* seed, which is consistency, not a dependency.
  2. `PUE_DEMO_TEMPLATE_SQL` — `BASELINE-ELECTRICAL-INCOMER` v1, `published`, `asset_type 'baseline'` (so `HEALTH_TEMPLATE_VERIFY_SQL`'s `unusable` check covers it), `domain 'electrical'`, `content = HEALTH_BASELINE_CONTENT` (the pinned incomer keeps its health band — E1.3's donut must not lose nine assets), `ON CONFLICT DO NOTHING`.
  3. `PUE_DEMO_TEMPLATE_POINTS_SQL` — copy `BASELINE-ELECTRICAL`'s **measured** `template_points` (7 rows, from `template_points`, not `asset_points`) plus the three derived rows via `unnest`: `kind 'derived'`, `formula`, `formula_dialect = $dialect` (`CALC_DIALECT_V2` passed as a parameter), `calc_trigger 'scheduled'`, `calc_interval_seconds 60`, `max_input_age_seconds NULL`, `min_coverage_ratio NULL`, `required false`, `sort_order 100/101/102`, `label`, `unit` — `ON CONFLICT (template_id, point_key) DO NOTHING`.
  4. `PUE_DEMO_PIN_SQL` — `UPDATE bms.assets SET template_id = incomer.id WHERE template_id = baseline_electrical.id AND id IN (SELECT asset_id FROM bms.asset_group_members WHERE role = 'incoming-supply')`. The role is the selector — ruling 1's "the site's `incoming-supply` asset" read off data rather than a code list. Only a baseline pin moves; a re-seed is a no-op; an operator's migration survives.
  5. `PUE_DEMO_VERIFY_SQL` — the three counts above, thrown on mismatch like `seedAssetTemplateHealth` (a FORCE-RLS write can drop rows without raising).
- `seed.ts` — the call, with a docblock naming the order dependency: after the groups (roles), after the point-key catalog (FK), after the health baselines (the copy source and the pin it moves).

**Idempotence under `compose up`** — each statement is `DO NOTHING`/predicated; the verifier's counts are the same on the first and the hundredth seed; the `source_kind <> 'computed'` predicate is what keeps the engine's own rows from feeding back into a baseline.

**Gate**: `pnpm db:seed` against the running database (**port 5433**, compose from the repo root) → `verifyHierarchySeed: ok`; then the scratch **cold start** (`down -v` or replay `001-bms-init.sql`; `roles && db:migrate && db:seed` **once**) → the same counts. The dev database cannot prove the order, for the reason `F3.41` Task 3 gives. **Commit**: `feat(db): demo PUE — IT_LOAD groups, rack_kw declared, and one incomer template per site`.

### Task 3b — The simulator's IT rack load is sized so the demo PUE is plausible — **Sonnet**

Added by the owner's ruling on open question 1 (§12), and **re-scoped by a second ruling on 2026-09-05** after the "one line" premise was measured false:

- The `kw: 80 + rand·400` initial at `index.js:103` is dead — `stepElec` overwrites `kw` with `v·i·pf/1000` on the first step (`:230`). A non-`CR-` (provincial) feeder's current random-walks in `[40, 520]` A (`:228`), so each of the eight provincial sites (3 feeders each) drifts between ~25 and ~360 kW. Western Cape's 21 `CR-*` feeders have fixed profiles (`crProfile`, `:111-136`) and sum to a stable ~41–44 kW.
- The IT `rackKw` initial (`:165`, 1.42 VW / 2.81 other) is clamped by its own walk at `:268` (`max` 3.1 for `NET`, 2.2 otherwise). Raising the initial alone does nothing after the first step.
- IT assets: one `XX-CR-NET-RACK` at each provincial site; six at Western Cape (`CR-VW-SRV-RACK`, `CR-NET-RACK`, `CR-VW-RACK-PDU-A/B`, `CR-NET-RACK-PDU-A/B` — the `CR-` prefix is the Western Cape marker).

**The ruling: IT load plus a narrower feeder band — three edits in `apps/sim/src/index.js`, nothing else.** (1) The IT initial at `:165`: Western Cape VW ≈ 3.2, Western Cape NET ≈ 6.2 (six racks Σ ≈ 28 kW → PUE ≈ 41/28 ≈ 1.5); provincial NET rack ≈ 77 (walk band below). (2) The IT walk clamp at `:268` to match: VW `[2.5, 4]`, WC NET `[5, 7.5]`, provincial NET `[70, 85]`. (3) The provincial feeder current band at `:228` from `[40, 520]` to `[200, 260]` A (and the `i` initial at `:102` inside it, `200 + rand·60`), so a provincial site sits in ~108–185 kW and its PUE in ~1.3–2.6, typically ~1.9. `crProfile` values, the CR current bands and every non-kW magnitude are untouched. **Test first**: a new `tests/f2.8-sim-it-load.test.ts` (wired into `typecheck:tests`; `tests/f4.73-simulator-tenant-context.test.ts` is the precedent for reading `index.js` as text) that extracts the three bands with regexes and asserts the arithmetic — max provincial IT below min provincial site kW (PUE ≥ 1 always), the provincial ratio band inside `[1.2, 2.8]` at the band edges, and the Western Cape ratio at the profile sum inside `[1.3, 1.7]`. Say the arithmetic in the commit body. **Commit**: `chore(sim): IT rack load and feeder band sized so the demo PUE is plausible`.

### Task 4 — The contracts go nullable and every consumer takes the null — **Opus**

One commit across `packages/shared`, `apps/api` and `apps/web`, forced by typecheck: `.nullable()` makes `s.pueEstimate.toFixed(2)` and the `ReportCell` assignment red in the same build. Ordered **before** the API change so no commit ships an API `null` that `checkResponse` (`apps/web/src/api/dashboard.ts:17`) would reject.

**Tests first.**

1. `apps/api/src/reports/reports.serialise.spec.ts` — a new case: `preview({ summary: { ...summary, pueEstimate: null } })` → the CSV line is exactly `PUE estimate,—,` (U+2014, the glyph `KpiTile` renders; not `-`, which the ADR 0026 guard would apostrophe-quote) and the XLSX cell is a string. `UNCHANGED_OUTPUT` untouched.
2. **New** `apps/web/src/lib/pue-tile.spec.ts` + `.test.ts` — a pure `pueTileProps(status, pue: number | null | undefined)` → `{ status, value, hint }`: `("ready", 1.42)` → `{ "ready", "1.42", "Σ site kW ÷ Σ IT kW, from the incomers' site_kw / it_kw" }`; `("ready", null)` → `{ "empty", null, "Not configured — no incomer in scope computes site_kw and it_kw" }`; `("loading", undefined)` → `{ "loading", null, … }`; `("error", …)` passes through.
3. **New jsdom pairs**, one assertion each on the exact rendered strings, in the `dashboards-page.spec.tsx` shape (QueryClientProvider + MemoryRouter, `vi.mock` of the API module):
   - `apps/web/src/pages/dashboard-page.spec.tsx` — mock `../hooks/use-executive-dashboard` returning `kpiQuery.data.pueEstimate: null` → the "PUE" tile shows `—` and the not-configured hint; `1.42` → `1.42`. Mock `../api/locations`.
   - `apps/web/src/pages/energy-page.spec.tsx` — mock `../api/energy-dashboard`'s three fetches.
   - `apps/web/src/components/reports-panel.spec.tsx` — mock `../api/reports`.
   Each wrapper carries `@vitest-environment jsdom` (ADR 0042 decision 2). `apps/web/vitest.config.ts` collects `src/**/*.test.tsx`; no CI edit.

**Then implement.**

- `packages/shared/src/contracts/dashboard.ts:126` and `operations.ts:20`: `pueEstimate: z.number().nullable()`. **Encoding**: a field-level `.nullable()` on an existing `z.object`; no intersection and no `.readonly()` is involved, so `z.intersection`-vs-`.merge()` is N/A — said so a reviewer does not go looking. `DashboardKpis`, `EnergyCentreSummary`, `EnergyReportPreview["summary"]` widen by inference; `apps/api/dist/*.d.ts` regenerate on build.
- `reports.serialise.ts:89`: `["PUE estimate", preview.summary.pueEstimate ?? "—", ""]`.
- **Delete** `apps/web/src/lib/pue-estimate.ts`. **Create** `apps/web/src/lib/pue-tile.ts`.
- `dashboard-page.tsx`: drop the import and `pueVal` (lines 6, 79-84); the tile takes `{...pueTileProps(kpiStatus, kpi?.pueEstimate)}`, label `PUE`, `stale` unchanged. The socket-driven live PUE was the curve; the measured ratio refreshes with `kpiQuery` (8 s).
- `energy-page.tsx:127-131` and `reports-panel.tsx:221-225`: the same three props; labels `PUE`; the old hints ("same curve as Executive Dashboard", "Prototype estimate") go.

**Gate**: `pnpm build` (the contract widening is what forces every consumer), `pnpm test`. **Commit**: `feat(contracts): pueEstimate is nullable, and every reader shows a dash with a reason`.

### Task 5 — The API reads Σ site_kw / Σ it_kw and the two curves die — **Opus**

**Tests first.**

1. **New** `apps/api/src/telemetry/pue-ratio.spec.ts` + `.test.ts` — the pure `pueRatioOf({ incomers, siteKw, itKw }): number | null`: `incomers 0` → `null`; `itKw 0` → `null` (ruling 4: no sentinel); `{ 9, 1447.3, 25.1 }` → `57.66`; `{ 1, 41.1, 12.6 }` → `3.26` (one site reduces to its own ratio, ruling 3); rounding to 2 dp like `round()`.
2. **New** `apps/api/src/telemetry/pue-ratio.integration.spec.ts` + `.test.ts`, gated by `requireIntegrationDb` (`apps/api/src/testing/integration-db-gate.ts:106`), fixture in a scratch org/location with three assets and `telemetry.point_values` rows, cleaned up in `afterAll`:
   - `latestPueRatio(pool, [a, b])`: A `site_kw 100 / it_kw 50`, B `300 / 100` → `400/150 = 2.67`; B alone → `3`; an asset C holding `site_kw` but no `it_kw` is **left out of both sums** (`HAVING COUNT(*) = 2`); `[c]` alone → `null`; `null` scope → every incomer; a scope excluding A → B's ratio.
   - `windowedPueRatio(pool, { level: "1h", start, end, assetIds })`: two hourly buckets per key → the ratio of window **means** (`avgExpr()` = `sum(sum_value)/sum(sample_count)`, never an average of averages — `dashboard.service.ts:677-680`). No `refresh_continuous_aggregate` call is needed: migration `0027` sets `materialized_only = false`, so the view unions the un-materialized tail (`:682-686`). Rows outside `[start, end]` do not count.
3. **`tests/f2.8-pue-curve-is-gone.test.ts` part (a) is written in Task 6 but run here first**, red: `estimatePue` and `1.22 + ` still exist in three files.

**Then implement.**

- **New** `apps/api/src/telemetry/pue-ratio.ts` (beside `point-aggregates.ts`, whose `avgExpr`/`aggregateRelation` it imports): `latestPueRatio(pool: Pool, assetIds: string[] | null)` — `DISTINCT ON (asset_id, point_key)` over `point_key IN ('site_kw','it_kw')` with the `$1::uuid[]` scope, paired per asset with `HAVING COUNT(*) = 2`, summed; `windowedPueRatio(pool, { level, start, end, assetIds })` — per `(asset_id, point_key)` window mean from `aggregateRelation(level)` with `bucket >= $1 AND bucket <= $2`, paired and summed the same way; both return `pueRatioOf(...)`. Docblock: ruling 3 verbatim; the pool is the caller's `FLEET_POOL` and the scope array is the containment (ADR 0043 Amendments 2/3, as `reports.service.ts:31-38` records); no bms table is joined; ~~no freshness bound, for parity with `kw_latest` at `dashboard.service.ts:447-451`~~ (**reversed 2026-09-06**, §11 decision 6: the latest read ignores rows older than `PUE_LATEST_MAX_AGE_SECONDS` = 900 s); the point keys are the stock incomer's (`electrical-feeder.ts`) and the demo template's.
- `dashboard.service.ts`: `kpis()` → `pueEstimate: await latestPueRatio(this.pool, assetIds ?? null)`; the two empty branches (`:436`, `:476`) → `pueEstimate: null`; return type `pueEstimate: number | null`. `energySummary()` → `windowedPueRatio(this.pool, { level, start: this.trailingStart(durationHours), end: new Date(), assetIds: assetIds ?? null })`, empty branch (`:667`) → `null`; **delete `estimatePue` (:541-547)**. `avg_kw` is no longer selected if nothing else reads it (it is not).
- `reports.service.ts`: `energySummary()` → `windowedPueRatio(this.pool, { level, start: range.start, end: range.end, assetIds })`, empty branch (`:146`) → `null`; **delete `estimatePue` (:332-338)**.
- Neither constructor changes, so `fleet-read-wiring.spec.ts`, `reports.service.rls.integration.spec.ts`, `rollup-conversion.integration.test.ts` and `point-aggregates.integration.test.ts` keep compiling.

**Gate**: `pnpm test` with the role URLs; the two `estimatePue` symbols are gone from `apps/api`. **Commit**: `feat(api): PUE is Σ site_kw / Σ it_kw over the incomers in scope, or null — both fitted curves deleted`.

### Task 6 — The repo-wide invariant, and its wiring — **Sonnet**

**Test first is the test.** **New** `tests/f2.8-pue-curve-is-gone.test.ts` — inline assertions (top-level `tests/` carve-out, §4.6), `@bms/shared` reached through `createRequire` exactly as `tests/adr-0055-calc-v2-invariants.test.ts:1-20` documents (a static import typechecks green on a hand-repaired symlink and dies in CI with TS2307):

- **(a)** No file under `apps/api/src` or `apps/web/src` (excluding `node_modules`, `dist`, and this test) contains `estimatePue` or `1.22 + `. Anti-vacuity: the scan found ≥ 300 files, and the regex matches the fixture string `1.22 + Math.min(0.45, totalKw / 12_000)`.
- **(b)** `electrical-feeder.ts` read as text: exactly three `derived(` calls; each option object carries `formulaDialect: CALC_DIALECT_V2` and `calcTrigger: "scheduled"`; the three `pointKey:` values are `site_kw`, `it_kw`, `pue`; `stockVersion: 2`; the three literals parse under v2 with the expected `crossRefs`/`refs` (the `sum(kw)@site` and `sum(kw)@group:IT_LOAD` keys — hand-written here and only here, because `crossRefKey` is the thing being pinned).
- **(c)** `packages/db/src/pue-demo-seed.ts` read as text: its `it_kw` formula reads `sum({rack_kw} @group('IT_LOAD'))` and its other two equal the stock literals — the divergence is one key and it is deliberate (§3.1).

**Wire it**: add `tests/f2.8-pue-curve-is-gone.test.ts` to the root `typecheck:tests` script (`package.json:15`); the `repo` vitest project globs `tests/**/*.test.ts`, and CI runs `pnpm typecheck:tests` (`ci.yml:82`) and `pnpm test:coverage` (`:181`) — nothing else to wire. **Commit**: `test: F2.8 invariant — the fitted PUE curve is gone and the incomer carries the three v2 points`.

### Task 7 — Step 6 against the running stack — see §8

---

## 6. Contract work

Two API responses change: `GET /api/v1/dashboard/kpis` (`dashboardKpisSchema`), `GET /api/v1/dashboard/energy/summary` (`energyCentreSummarySchema`), and through the latter's reuse `GET /api/v1/reports/energy/preview` (`energyReportPreviewSchema.summary`). Each is already `z.infer`red (§4.8, ADR 0030). The change is `pueEstimate: z.number().nullable()` on two existing `z.object` schemas — **no new schema, no intersection, no `.readonly()`**, so the three encodings §4.8 warns about are N/A. The OpenAPI document is generated from these schemas at runtime; `tests/adr-0029-openapi-contract.test.ts` scans controllers rather than a committed snapshot, so nothing to regenerate. ADR 0026 line 95 lists `pueEstimate` among the numeric CSV cells — it is now `number | "—"`; named in §10.

---

## 7. Blast radius

**Forced to change** (CodeGraph + the reads in §3):

| Symbol / file | Why |
|---|---|
| `DashboardService.estimatePue` (`dashboard.service.ts:541`) — callers `kpis` (:487), `energySummary` (:736); no covering test | deleted; both callers rewired (Task 5) |
| `ReportsService.estimatePue` (`reports.service.ts:332`) — caller `energySummary` (:204); no covering test | deleted (Task 5) |
| `apps/web/src/lib/pue-estimate.ts` → `dashboard-page.tsx:6,81` | deleted; replaced by `pue-tile.ts` (Task 4) |
| `pueEstimate` readers: `reports.serialise.ts:89` + `reports.serialise.spec.ts:45`, `dashboard-page.tsx:83`, `energy-page.tsx:130`, `reports-panel.tsx:224` | take `null` (Task 4) |
| `contracts/dashboard.ts:126`, `contracts/operations.ts:20` | `.nullable()` (Task 4) |
| `ELECTRICAL_FEEDER` → `ELECTRICAL_STOCK_ASSET_TEMPLATES` → `STOCK_ASSET_TEMPLATE_CATALOG` | the three points + `stockVersion 2` (Task 2) |
| `stock-catalog.spec.ts` (`checkEntry` derived branch, feeder block), `stock-catalog-deferrals.spec.ts:637-645`, `tests/adr-0055-calc-v2-invariants.test.ts` part (b) | each refuses a v2 stock point today (Task 2) |
| `tests/f2.13`, `tests/f3.38`, `tests/f3.39` floors | +3 keys (Task 1) |
| `ELECTRICAL_CLASS_POINT_KEYS`, `UNIT_BY_KEY` | +3 (Task 1) |
| `seedAssetGroups` (callers `seed.ts:177,207`), `asset-groups-seed.spec.ts` | `demoGroupCodesForAsset`, `IT_LOAD` (Task 3) |
| `HEALTH_TEMPLATE_POINTS_SQL` + `asset-template-health-seed.spec.ts` | the `computed` predicate (Task 3) |
| `verifyHierarchySeed` (callers `seed.ts:246`, `verify-hierarchy-cli.ts`; the compose `migrate` boot gate) | three ESKOM counts (Task 3) |
| `seed.ts` | one call (Task 3) |
| `point-fields.ts:111-117` docblock | first caller exists (Task 2) |
| `package.json` `typecheck:tests` | the new `tests/` file (Task 6) |
| **Missed by this table, found in the build and the reviews:** `asset-templates-stock.integration.spec.ts:359-361` (pinned the feeder at v1/33 — `ec57cac`); `asset-template-stock-view-page.spec.tsx:31,125` (prose calling the v1 fixture "the live entry"); `apps/sim/src/index.js` provincial feeder band (Task 3b — moves `totalKw`, peak demand and `current_a` on eight sites); `BASELINE-IT` now declares `rack_kw` from the first boot (the 14 `unmapped` rows are catalog rows the health seed copies; no threshold rule reads it, so score and band do not move); `dashboard.service.ts` `kpis()` docblock (a silent site also yields `null` since the 900 s bound) | each fixed in-row |

**Reads the changed symbols and needs no edit — each verified:**

- `AssetTemplatesStockService.list/import` — spreads the catalog; the import's `assertPointKeysActive` passes once Task 1's keys are seeded (a tenant database gets them at its next `compose up`).
- `DashboardService`/`ReportsService` constructors and their integration suites (`fleet-read-wiring.spec.ts`, `reports.service.rls.integration.spec.ts`, `rollup-conversion.integration.test.ts`, `point-aggregates.integration.test.ts`) — no signature change.
- `apps/web/src/api/{dashboard,energy-dashboard,reports}.ts` — `checkResponse` against the widened schemas.
- `CalcDefinitionsService` / `CalcScopeService` / `CalcSchedulerService` — they evaluate whatever `template_points` declares; the demo template is just more rows (cache TTL 60 s, `calc-definitions.service.ts:12-13`).
- `seedScopedDemoUsers` — grants on ESKOM groups by existing codes; `IT_LOAD` is additive. `control-room-access.ts:40` keeps reading `it-rack`.
- `HEALTH_TEMPLATE_PIN_SQL` (`template_id IS NULL` guard) and `HEALTH_TEMPLATE_VERIFY_SQL` (`t.domain = a.domain`) — the incomer template is electrical; `unpinned` stays 0.
- `AssetHealthService` — the incomer template carries the same bands; E1.3's `bandCounts` unchanged.
- `tests/f3.37` `roles.size === 28`, `tests/adr-0043-tenant-columns` — no role, no column.
- **No test pins an exact `bms.point_keys`, ESKOM `asset_points`, `asset_groups` or `asset_templates` count** (grepped `tests/`, `packages/db/src`, `apps/api/src`; the only `604` hit is a facility point label).

---

## 8. Verification — §4.6, layer by layer, N/A named

**Suite claims already held** (say which, per the verify skill §4.1): the curve is gone (Task 6a); the stock entry's shape and dialect (Task 2 specs, Task 6b); the ratio arithmetic and the half-pair exclusion (Task 5 specs); the null rendering (Task 4 jsdom specs); seed order on a cold database (Task 3 verifier under CI).

**Database — required.** Compose from the repo root, Postgres on **5433**.
1. `docker compose up -d migrate` (re-seed) → `verifyHierarchySeed: ok`; `bms.point_keys` = 607; `BASELINE-ELECTRICAL-INCOMER` published with 7 measured + 3 derived (`formula_dialect = 'bms-calc-v2'`, `calc_trigger = 'scheduled'`, 60 s, `min_coverage_ratio NULL`); 9 incomers pinned; 9 `IT_LOAD` groups / 14 members; 14 `rack_kw` rows; `BASELINE-ELECTRICAL` still declares exactly its 7 keys.
2. **Both directions of the `computed` predicate**: after step "engine" below has written `site_kw`/`it_kw`/`pue` rows, run `pnpm db:seed` again → `BASELINE-ELECTRICAL` still 7 keys, the incomer template still 10.
3. **Cold start on a scratch database** — `down -v` or replay `001-bms-init.sql`, then `roles && db:migrate && db:seed` **once** → the same counts. A `DROP DATABASE` does not fire the init hook.

**Engine + API — required.** `docker compose build api` restarts nothing; `up -d api`, and check the container's `Created` time against the commit. **Start the simulator** (`docker compose --profile sim up -d sim`) — ESKOM telemetry stopped on 2026-09-04 15:57, so without it every formula refuses `stale_input` and the demo shows a dash for a true reason.
- Within the 60 s definitions TTL plus two 10 s ticks: `SELECT asset_id, point_key, time, value FROM telemetry.point_values WHERE point_key IN ('site_kw','it_kw','pue') ORDER BY time DESC` shows 9 × 3 rows on bucketed (`:00`) times; `bms.asset_points` has 27 `computed` rows; `/metrics` `bms_api_calc_active_formulas` rose by 27 and `bms_api_calc_skipped_total{reason="no_members"}` did not move.
- `GET /api/v1/dashboard/kpis` as `admin@bms.local` → `pueEstimate` equals a psql Σ/Σ over the nine incomers to 2 dp; `energy/summary?window=24h` → the windowed value; `reports/energy/preview` → the same shape; `export.csv` → `PUE estimate,<n>,`. **The HTTP reads go through `browser-verifier` as `fetch` from the SPA tab — the API is OIDC with no password grant.**
- **Scope**: `wc-admin@bms.local` (location-scoped) → `pueEstimate` equals that site's incomer's own `pue` row (ruling 3's single-site reduction). The PHE organization admin → `null`, and the CSV row reads `PUE estimate,—,`.
- **Other direction**: stop the sim → after 300 s `bms_api_calc_skipped_total{reason="stale_input"}` rises and no new rows land; the KPI keeps the last written ratio for up to `PUE_LATEST_MAX_AGE_SECONDS` (900 s) and then reads `null` — the dash with the "not configured" hint — because the owner reversed the no-bound decision on 2026-09-06 (§11 decision 6). A dash after 15 minutes of silence is the correct reading, not a regression. The windowed reads keep their window's mean.

**Browser — required, via `browser-verifier`, `javascript_tool` exact-string assertions, never `computer` screenshots.** Hard-reload and confirm the served asset hash changed first. Dashboard tile labelled `PUE` shows the 2-dp string equal to the fetch above; energy page and reports panel the same; as the PHE admin all three show `—` and the text `Not configured — no incomer in scope computes site_kw and it_kw`; the asset calc-points page for `CR-UTILITY-11KV` lists three derived rows with runtime `written`.

**N/A, with the reason:** MQTT/ingest — untouched, `apps/ingest` reads neither the catalog nor the contracts. Migrations — none (ruling 5). Socket.IO — a computed write notifies exactly as any `v1` write does (F2.9 §Verification). **Simulator — not N/A:** the owner's rulings on question 1 (§12) made Task 3b change `apps/sim/src/index.js`, and the narrower provincial feeder current band (200–260 A, was 40–520) moves every `current_a` and `kw` reading on the eight provincial sites' non-`CR-` feeders, so the estate `totalKw` and peak-demand tiles and the E1.3 donut on the running stack must be read as a changed layer, not only the PUE tile. The `CR-*` profile branch and the seeded `current_a` breaker rules are on Western Cape profile assets and do not move.

---

## 9. Commit shape

One per task, each green before the next:

1. `feat(shared): site_kw, it_kw and pue join the electrical class vocabulary`
2. `feat(api): electrical-feeder v2 — PUE as three bms-calc-v2 derived points on the incomer`
3. `feat(db): demo PUE — IT_LOAD groups, rack_kw declared, and one incomer template per site`
3b. `chore(sim): IT rack load and feeder band sized so the demo PUE is plausible` (the owner's two rulings on §12 question 1)
4. `feat(contracts): pueEstimate is nullable, and every reader shows a dash with a reason` (cross-package, forced by typecheck — the message says so)
5. `feat(api): PUE is Σ site_kw / Σ it_kw over the incomers in scope, or null — both fitted curves deleted`
6. `test: F2.8 invariant — the fitted PUE curve is gone and the incomer carries the three v2 points`

One PR: `feat(pue): F2.8 — PUE from bms-calc-v2 derived tags on the incomer`. Review set: `code-reviewer`, `security-reviewer`, `agents-compliance-reviewer`, plus `migration-reviewer` because the diff touches `packages/db` (seed and verifier, no SQL file). Run the sweep after merge too.

**Separate `chore(agents):` PR, never on this branch** (§9.10): `docs/BACKLOG.md` `F2.8` → ✅ with the five rulings repeated in the closure row, the `F2.9` row's "unblocks `F2.8`" and §8 row 6, `AGENTS.md:95` and §6 (the paragraph ending "`F2.8` is now startable"), `docs/roadmap.md:90,158,405,1876-1882`, and §10 below. Board republish in two stages, in a fresh session.

---

## 10. Prose that becomes false when this lands

- **ADR 0037** *Consequences* — "`F2.8` is not reachable on `bms-calc-v1`… this ADR does not fix that" (history, keep) and "**No `F2.8` wiring.** The PUE path… is untouched" (now false).
- **ADR 0050** Context — "it should be driven by `F2.8`"; *Not in this ADR* — "`F2.8` still needs its own answer".
- **ADR 0055** Context — "What ships today is the thing `F2.8` exists to remove… none of the three has a covering test"; *Consequences* — "`F2.8` does not close until all three copies are gone".
- **ADR 0052** decision 9 — "Nothing is seeded, and `BASELINE-*` are untouched. The four seeded baselines…" — a fifth `BASELINE-*` row now exists and the seed writes a `v2` formula; still no pack import.
- **ADR 0026** line 95 — `pueEstimate` listed as a numeric CSV cell.
- `AGENTS.md:95` ("`F2.8` is unblocked"), `:413` (calc engine "evaluates `bms-calc-v1` formulas"), `§6` ("`F2.8` is now startable"). `docs/roadmap.md:90,158,405` ("PUE estimate"), `:1876-1882`.
- `docs/BACKLOG.md` `F2.9` row ("unblocks `F2.8`"), §5 rows 826/877, §8 row 6.
- `point-fields.ts:114-117` (no caller yet) — fixed in Task 2. `electrical-feeder.ts:52` ("Zero `kind: "derived"` points") and the `losses_pct` bullet — Task 2. `stock-catalog-deferrals.spec.ts:520-528` — Task 2. `asset-template-health-seed.ts:20-28` ("Four templates") — Task 3. `calc.module.ts:34-35` ("no seed data of its own") — now a seeded `v2` formula exists; note in Task 3's docblock. `calc-definitions.service.ts:126` ("every seeded asset is hand-created") — already false since `F4.75`; not this row's, named for the sweep.

---

## 11. Decisions taken at step 3, so a later reader does not re-open them

1. **The demo pins, it does not override.** Forced by `resolveWritableContext` and the loader's INNER JOIN (§3); an override can only re-parameterise a declared point.
2. **`IT_LOAD` is a second group, `it-rack` stays.** Two web/migration readers name `it-rack`; ruling 2 reserves the code, it does not rename.
3. **The incomer is selected by `role = 'incoming-supply'`**, not by a code list — ruling 1 read off data, and `demoRoleForAsset` already writes it.
4. **`minCoverageRatio: null`** on both stock aggregates — ADR 0055 decision 11's default; relaxation is the importing tenant's visible edit.
5. **60 s interval**, both entries — the KPI refetches at 8 s and the energy page at 60 s; a 27-formula sweep every minute is trivial; ADR 0055 decision 10's latency cost is one minute.
6. ~~**No freshness bound on the KPI read**~~ — **superseded by the owner's ruling of 2026-09-06: bound the latest read at 15 minutes.** The decision as taken rested on "the ribbon's `Stale` badge is the page-level signal", and the review of this branch measured that claim false — the badge is derived estate-wide from the last Socket.IO `kw` tick (`use-executive-dashboard.ts:68-71`), so a single silent site never moves it, and neither `energy-page.tsx` nor `reports-panel.tsx` passes `stale` at all. `latestPueRatio` now drops any `site_kw`/`it_kw` row older than `PUE_LATEST_MAX_AGE_SECONDS = 900` — three times the engine's 300 s default `maxInputAgeSeconds` — so a silent site leaves both sums and an entirely silent scope returns `null` and shows the dash. `windowedPueRatio` keeps only its caller's `[start, end]`.
7. **CSV dash is U+2014**, the glyph `KpiTile` renders, and the `PUE estimate` label stays to keep ADR 0026's byte pin intact.
8. **`HEALTH_TEMPLATE_POINTS_SQL` excludes `computed` rows** — found by reading the seed against `CalcWriteService`; without it the engine's outputs become measured declarations on the next boot.

---

## 12. Open questions for the owner — before Task 3

**A third ruling on 2026-09-06, after the reviews:** the latest-value KPI read is **bounded at 15 minutes** (`PUE_LATEST_MAX_AGE_SECONDS` = 900 s, three times the engine's 300 s stale-input default). The code review showed decision 6's compensating control did not exist — the ribbon's `Stale` badge is estate-wide and the energy and reports tiles carry none — so a site whose engine had stopped hours earlier stayed in both sums with no signal. A silent site now drops out of both sums; if every site is silent the tile shows the dash. The windowed reads are unchanged. Options put: bound at 15 min (chosen) · keep parity with `totalKw` and note it · add a `pueAsOf` timestamp to the contract.

**Both resolved by the owner on 2026-09-05, after the plan was written:**

1. **Retune the sim in this row** — option (b). One line in `apps/sim/src/index.js` raises the IT `rackKw` so each demo site lands near 1.5; `totalKw` and every other magnitude stay. It is **Task 3b** below, its own commit `chore(sim): IT rack load sized so the demo PUE is plausible`, built after Task 3 and before Task 4. §1's "no change to `apps/sim`" bullet and §8's simulator N/A line are superseded by this ruling.
2. **Effort re-set to 4–5 in the sweep.** The `chore(agents):` PR after merge changes the row's Effort cell from `2` to `4–5`, and the closure row says why the `2` was wrong (the override cannot carry the demo; three build-time guards refuse a v2 stock point by design).

The questions as they were put:

1. **The demo number is honest and absurd: ~58 estate-wide, 40–300 per RSMOC site, 3.3 at Western Cape** (§3). The sim's IT load is ~2 % of its electrical load. Options: **(a)** ship it as measured — the ruling's letter (no fallback), the sim is not this row's, and a follow-up row retunes `apps/sim`; **(b)** in this row, raise `rackKw` in `apps/sim/src/index.js:165` (and nothing else — `totalKw` is untouched) so each demo site lands near 1.5, one line and one commit `chore(sim): IT rack load sized so the demo PUE is plausible`. *Recommend (b)*: an executive tile reading `57.66` teaches a visitor the feature is broken, and the tuning is a simulator number, not a product fallback. Either way the plan above is unchanged.
2. **Effort re-set.** The row carries `2`. Measured against the tasks: T1 0.25 · T2 0.75 · T3 1.0 · T4 0.75 · T5 0.75 · T6 0.25 · T7 0.5 = **4.25**, plus one review-fix loop ≈ **4–5**. The `2` predates the discovery that the override cannot carry the demo (§3) and that three build-time guards refuse a v2 stock point by design.

## Gates still owed

- **No ADR gate** — ruling 5.
- **Merge approval is the human's** (step 7), then the separate `chore(agents):` PR.
- Open questions 1 and 2 above, before Task 3 starts.
