# E8.4 — Credential key rotation and unconfigured-key visibility (ADR 0062)

Branch `E8.4-credential-key-rotation`, base `main@13b32232`, ADR committed at
`4031534e`. Planned on Fable 2026-09-10. ADR 0062 is **Accepted**; the step-2
gate holds.

## 1. Goal

Implement ADR 0062 decisions 1–9 and 11: a shared key resolver with a two-key
window (`CREDENTIAL_ENCRYPTION_KEY`, `CREDENTIAL_ENCRYPTION_KEY_PREVIOUS`,
`CREDENTIAL_ENCRYPTION_KEY_VERSION`), used by both `CredentialCryptoService` and
the ingest's `rtu-config.js`; `decrypt` selects a key by the stored version and
refuses any other; the version travels with every ciphertext carrier (RTU config
row, channel secret row, onboarding draft blob, ingest binding SQL); both
applications refuse to boot on a dead window; a `pnpm --filter api
rotate-credentials` command re-encrypts both tables as `bms_fleet`; the
onboarding merge stops claiming a credential it did not store; and the ingest
names which credential it actually uses.

No schema change, no new npm package, no gate that depends on a deployed key.

**Out of scope, deliberately.** Retiring the `MQTT_USERNAME`/`MQTT_PASSWORD`
fallback (decision 10, blocked on data — `E8.4` stays open on it); a three-key
window; a JSON key map; pino `authorization` redaction (E8.3 deferred it here,
ADR 0062 does not take it); binding a draft credential to an endpoint rather
than an RTU code; any `AGENTS.md`, `docs/BACKLOG.md` or `docs/roadmap.md` edit
(separate `chore(agents):` PR, §9.10); exposing plan warnings on the ingest
health endpoint. API contract work is **N/A** — `credentialsSet` already exists
in the onboarding contract, `credentialSource` is ingest-internal, and the
rotation report is CLI stdout.

## 2. Facts the plan rests on

Measured 2026-09-10 in this worktree. Each one changed a design choice.

- **`apps/api` compiles with `moduleResolution: "node"` (node10), which ignores
  the `exports` map.** `packages/shared/src/index.ts:60-66` already records
  this. So the API imports the resolver from the **barrel** and the ingest
  (NodeNext) imports **`@bms/shared/credential-keys`**. One file, two routes.
- **`packages/shared` has no `@types/node` reachable.** The root
  `node_modules/@types` holds only eslint; the package's own holds `zod` and
  `typescript`. Any `Buffer` or `process` reference fails
  `tsc -p packages/shared/tsconfig.json` and `pnpm --filter @bms/shared build`.
  The resolver is therefore written over `Uint8Array`, takes an injected base64
  decoder, and takes `env` as a parameter. This also keeps the barrel safe for
  `apps/web`, which bundles it.
- **`check-dependency-adr.mjs` denies only added lines shaped like a dependency
  specifier** (`scripts/checks/dependency-spec.mjs`). An `exports` entry or a
  `scripts` entry does not match, so the Edit tool works on both manifests;
  `pnpm pkg set` is the fallback.
- **JSDoc-typed parameters on a `.js` function make arity compiler-checked for
  TypeScript callers** under `allowJs`, even with `checkJs: false` — probed:
  `TS2554: Expected 3 arguments, but got 2`. This is what turns the
  `main.ts → decryptCredentials` forwarding into a gated claim.
- **`scripts/checks/style-hygiene.mjs:39` rejects `console.log/debug/info`** on
  added lines. The CLI writes with `process.stdout.write`.
- **The API has no central env validator.** `main.ts` calls
  `NestFactory.create(AppModule)` with `abortOnError` defaulting true and
  providers instantiated eagerly, so a provider constructor that throws *is* a
  refused boot. `DatabaseModule` already uses this shape. The ingest's validator
  is `readHostConfig(env)` in `apps/ingest/src/host/config.ts`.
- **`tests/f4.108-service-parses-are-guarded.test.ts:112` allow-lists the one
  `JSON.parse` in `credential-crypto`.** Keep exactly that one; the rotation
  walk calls `decrypt` and never parses.
- **No `tests/` file needs to change**, so neither the root `typecheck:tests`
  list nor `.github/workflows/ci.yml` needs an edit. Every new spec lives under
  `apps/**` or `packages/**` and the existing vitest projects discover it.
- **Coverage thresholds `73.4 / 69.4 / 75.1 / 73.5`** against a last measurement
  of `74.14 / 70.17 / 75.89 / 74.22`. The only deliberately uncovered new lines
  are the CLI entry (~30) and its module (~10). **Never lower a threshold.**
- `withRollback` exists at `alarm-kb.integration.spec.ts:28`; services accept
  `tx: BmsTx | BmsDb` with precedent at `dashboard-point-scope.ts:49`.
- `fleet-read-wiring.spec.ts`'s `injectedToken` reads
  `SELF_DECLARED_DEPS_METADATA`, which `@Inject()` writes without
  `emitDecoratorMetadata`, so a DI token can be pinned without a Nest module.

## 3. Serial, not parallel

E8.4 is not a ⭐ enabler, but **Task 1 defines the seam every later task hangs
off**, so it is built first and hands-on. Afterwards the API chain (2–4, 6) and
the ingest chain (5) touch disjoint files, but each is about six files and the
cold-start cost of a second agent exceeds the saving. One branch, built in
order.

Model per unit, per the operating-model ladder: **Task 1 Fable** (seam),
**Task 6 Fable** (production code connecting as `bms_fleet`/BYPASSRLS),
**Tasks 2, 4, 5 Opus**, **Tasks 3 and 7 Sonnet**.

## 4. Build order and the shared-package rule

`pnpm build` runs shared → db → api → web → ingest, and both apps consume
`packages/shared/dist`. Vitest resolves `@bms/shared` to `dist` too. Therefore:

- After Task 1's implementation and **before any typecheck or test** in
  `apps/api` or `apps/ingest`, run `pnpm --filter @bms/shared build`. Re-run it
  after every later edit to `credential-keys.ts`. **A stale `dist` makes every
  dependent spec assert against the old resolver and looks like a code bug.**
- Run `pnpm typecheck:tests` after every signature change (Tasks 2, 4, 5).
  `pnpm build` excludes specs and `pnpm test` strips types; only that script
  compiles a spec against the real types (§4.6).
- Filtered runs use the root runner:
  `pnpm exec vitest run --project <api|ingest|shared> <name-filter>`.
  `pnpm --filter api test` exits 0 having run nothing.
- Integration specs need `DATABASE_URL`, e.g.
  `postgres://bms_owner:bms_owner_dev@localhost:5433/bms` — the compose override
  remaps Postgres to 5433 on this machine. They fail closed under `CI` without
  it.

## 5. Task 1 — the key resolver in `packages/shared` (decisions 1, 2, 4, 5)

**Files.** Create `packages/shared/src/credential-keys.ts`, `.spec.ts` and
`.test.ts`. Modify `packages/shared/src/index.ts` (add
`export * from "./credential-keys";` with the node10 reason in a comment, in the
style of the `./calc-dsl` re-export). Modify `packages/shared/package.json` —
add the `./credential-keys` subpath with `types`/`import`/`require`.

**Public surface — the seam. Transcribe exactly.**

```ts
export const CREDENTIAL_KEY_ENV = {
  current: "CREDENTIAL_ENCRYPTION_KEY",
  previous: "CREDENTIAL_ENCRYPTION_KEY_PREVIOUS",
  version: "CREDENTIAL_ENCRYPTION_KEY_VERSION",
} as const;
export const CREDENTIAL_KEY_BYTES = 32;
export type CredentialKeyEnv = { readonly [name: string]: string | undefined };
export type Base64Decoder = (base64: string) => Uint8Array;
export type LoadedCredentialKeys = {
  readonly currentVersion: number;      // >= 1; defaults to 1; present even when `current` is null
  readonly current: Uint8Array | null;  // null = unconfigured
  readonly previous: Uint8Array | null; // null = no rotation window
};
export class CredentialKeyConfigError extends Error { override readonly name = "CredentialKeyConfigError"; }
export class CredentialKeyVersionError extends Error {
  override readonly name = "CredentialKeyVersionError";
  constructor(readonly storedVersion: unknown, readonly loadedVersions: readonly number[]);
}
export function resolveCredentialKeys(env: CredentialKeyEnv, decode: Base64Decoder): LoadedCredentialKeys;
export function loadedKeyVersions(keys: LoadedCredentialKeys): readonly number[];
export function keyForVersion(keys: LoadedCredentialKeys, storedVersion: unknown): Uint8Array;
```

**`resolveCredentialKeys`, in order.**

1. An empty string is unset, for all three variables — compose passes
   `${VAR:-}`.
2. `VERSION`: unset → `1`; else must match `/^\d+$/`, be a safe integer and be
   `>= 1`, else `CredentialKeyConfigError` naming the variable and the value.
3. `KEY`: unset → `current: null`; else decode; a length other than 32 →
   `CredentialKeyConfigError` ("must decode to 32 bytes") — **ruling 1**.
4. `PREVIOUS`: unset → `null`; else decode; wrong length →
   `CredentialKeyConfigError`; set while `currentVersion === 1` →
   `CredentialKeyConfigError` whose message contains `version 0` (decision 5);
   set while `KEY` is unset → `CredentialKeyConfigError` — **ruling 2**.

**`keyForVersion`.** A non-integer `storedVersion` (`null`, `NaN`, a string) →
`CredentialKeyVersionError`. `=== currentVersion` → `current`, or
`CredentialKeyConfigError("CREDENTIAL_ENCRYPTION_KEY is required")` when
`current` is null (today's message). `=== currentVersion - 1 && previous !==
null` → `previous`. Anything else → `CredentialKeyVersionError(stored,
loadedKeyVersions(keys))`, message `credential key version <stored> is not
loaded (loaded: <a, b>)`. Integers only, never bytes. **There is no path that
tries a second key.**

**Tests first.** One exported function per claim, each its own `it()`. The
decoder is a table — `decode("alpha")` → 32 bytes of `0x01`, `decode("beta")` →
32 of `0x02`, `decode("short")` → 16 — so the spec never needs `Buffer` or
`atob`.

| Assertion | Reddens when |
| --- | --- |
| unset env → `{ currentVersion: 1, current: null, previous: null }` | the default version is not 1 |
| all three `""` → same as unset | the empty-string check is dropped |
| `KEY=alpha` → `current` equals `decode("alpha")` byte-for-byte (**positive control** for the decoder wiring), `previous` null | the resolver returns a fixed key or ignores env |
| `KEY=alpha, PREVIOUS=beta, VERSION=3` → version 3, `previous` equals `decode("beta")` | the previous key is ignored or the version is not parsed |
| `VERSION` in `["0","-1","abc","1e3","1.5"]` → `CredentialKeyConfigError` naming the variable (table; the failing input is in the message — a loop of asserts reddens on its first failure only) | the digit regex or the `>= 1` bound is dropped |
| `KEY=short` → error containing `32 bytes` | the current-key length check is dropped (**ruling 1**) |
| `KEY=alpha, PREVIOUS=short, VERSION=2` → error | the previous-key length check is dropped |
| `KEY=alpha, PREVIOUS=beta`, VERSION unset → error containing `version 0` | decision 5's guard is dropped; the row above is its positive control |
| `PREVIOUS=beta` alone → error | **ruling 2** is dropped |
| `loadedKeyVersions` → `[3,2]` with a previous key, `[3]` without | the list includes `v-1` with no previous key — the error message would then lie |
| `keyForVersion(keys{3:alpha,2:beta}, 3)` is alpha; `(…, 2)` is beta (two `it()`s) | the slots are swapped |
| `(…, 1)` throws `CredentialKeyVersionError`, `storedVersion === 1`, `loadedVersions [3,2]`, message contains `"1"` and `"3, 2"` | an unknown lower version falls through to the previous key |
| `(…, 4)` throws the same class | an unknown higher version falls through to current |
| `(…, null)`, `(…, NaN)`, `(…, "3")` each throw (table, input in the message) | a loose comparison or a `Number()` coercion lets a non-integer through — **NaN makes every `===` false, so this must be asserted, not reasoned** |
| `keyForVersion({1, null, null}, 1)` throws `CredentialKeyConfigError` containing `is required` | an unconfigured current slot returns `null` |
| `keyForVersion(keys{3:alpha, previous null}, 2)` throws with `loadedVersions [3]` | `previous === null` is not checked before the `v-1` branch |

Use `err.name === "…"`, never `instanceof`, in every spec importing these
classes under vitest — F4.108: two module instances.

**Steps.** Spec and wrapper → red. Implement → green. `pnpm --filter @bms/shared
build` and confirm `dist/credential-keys.{js,d.ts}`. `pnpm typecheck:tests` (the
shared leg proves no Node global crept in). Commit.

## 6. Task 2 — `CredentialCryptoService` (decisions 3, 4, 5)

**Files.** `apps/api/src/security/credential-crypto.service.ts`, `.spec.ts`,
`.test.ts`.

**Changes.** Import `resolveCredentialKeys`, `keyForVersion` from the barrel;
`const decode: Base64Decoder = (s) => Buffer.from(s, "base64")`. A private
`static load()` calls the resolver **per call** in
`encrypt`/`decrypt`/`isConfigured`/`currentKeyVersion` (today's per-call env
read; specs mutate `process.env` after construction and rely on it) and **once
in the constructor** for its throw. Delete `private readonly keyVersion = 1` and
`getKey()`. `isConfigured()` → `try { return load().current !== null } catch
{ return false }`. New `static currentKeyVersion(): number`. `encrypt()` stamps
`keys.currentVersion`. `decrypt(ciphertext, iv, keyVersion: number | null)` →
`keyForVersion(load(), keyVersion)`; the single `JSON.parse` and the
object-shape guard stay.

The constructor docblock records why a throw there is the refused boot, and that
the service is provided in `notifications.module.ts:47` and
`admin.module.ts:84`.

**Tests first.** Each its own `it()`; each saves and restores the three env vars
in `finally`; each generates keys with `randomBytes(32)` and constructs the
service **after** setting env — **a module-scope service or a shared ciphertext
would hide the read-at-construction-vs-per-call mutation class.**

| Assertion | Reddens when |
| --- | --- |
| existing `runCredentialCryptoTests`, updated to pass `enc.keyVersion` | — |
| `KEY=A, VERSION=3` → `encrypt().keyVersion === 3` | the pin survives |
| `currentKeyVersion()` is 5 with `VERSION=5`, 1 when unset (two `it()`s) | the static reads a literal |
| encrypt under `KEY=A, VERSION=2`; then `KEY=B, PREVIOUS=A, VERSION=3`; `decrypt(c, iv, 2)` round-trips | decrypt always uses the current key. **Also the positive control for the two rows below: the ciphertext is genuinely decryptable under the loaded previous key.** |
| the same ciphertext labelled `1` → throws, `err.name === "CredentialKeyVersionError"`, message contains `"1"` and `"3, 2"` | **decision 4's guess refusal.** A try-both-keys implementation *succeeds* here because A's GCM tag verifies. This assertion is the one that distinguishes selection from guessing. |
| ciphertext under B (current, v3) labelled `2` → throws, and `err.name !== "CredentialKeyVersionError"` | decrypt falls back to current, or guesses — either makes this call succeed |
| `decrypt(c, iv, null)` throws `CredentialKeyVersionError` | a `?? 1` sneaks in at the API layer (the channels column is nullable) |
| `KEY=A, PREVIOUS=B`, VERSION unset → `new CredentialCryptoService()` throws containing `version 0` | the constructor does not call the resolver |
| `KEY=A, PREVIOUS=B, VERSION=2` → constructs, `isConfigured()` true (positive control) | the constructor over-refuses |
| `KEY=A, PREVIOUS=<16 bytes>` → constructor throws | decision 5's length rule is lost at the API layer |
| env unset → constructs, `isConfigured()` false | the constructor refuses an unconfigured key, killing `onboarding-chat-caps.spec.ts:176` |

`pnpm typecheck:tests` here is the run that lists every `decrypt` caller still
passing two arguments — expect `channels.service.ts:914` to fail until Task 4.

## 7. Task 3 — the version travels with the draft blob and the RTU row (decisions 3, 8)

**Files.** `onboarding-redaction.ts` and its spec;
`onboarding-commit.service.ts` (line 346 area) and its RLS integration spec;
`onboarding-chat.service.ts` (737–764); new
`onboarding-chat-credentials.spec.ts`/`.test.ts`.

**Changes.** `EncryptedBlob` becomes `{ c: string; iv: string; v?: number }` and
is **exported** — the chat service repeats the shape inline. `ownBlob` accepts
an absent `v` and a safe integer `>= 1`; any other present `v` makes the blob
absent, the treatment a malformed `c` already gets — **and it must carry `v`
through**, because `reconcileSecrets` rebuilds `kept` from `ownBlob` on every
merge and would otherwise strip the version from every stored blob.
`attachEncryptedCredentials(…, keyVersion: number)` writes `v`.
`readEncryptedCredentials` returns `keyVersion = entry.v ?? 1` — decision 3: a
pre-change blob is version 1.

`onboarding-commit.service.ts`: take `keyVersion = enc.keyVersion` in the
`enc && isConfigured()` branch, else `CredentialCryptoService.currentKeyVersion()`;
the insert uses that variable. The literal `1` is gone.

`onboarding-chat.service.ts`: forward `enc.keyVersion`; **delete** the
`else if (…) credentialsSet: true` branch (decision 8); widen the inline
`_secrets` types.

**Tests first.**

`onboarding-redaction.spec.ts`:

| Assertion | Reddens when |
| --- | --- |
| `attachEncryptedCredentials(draft, 0, CT, IV, 4)` → `_secrets.R1.v === 4` | `v` is not written |
| `readEncryptedCredentials` of `{c,iv,v:7}` → `keyVersion === 7` (positive control) | `v` is not read |
| of `{c,iv}` → `keyVersion === 1` | the default becomes the current version or `undefined` — the return type `number` also catches `undefined` at `typecheck:tests` |
| `{…,v:"2"}`, `{…,v:0}`, `{…,v:1.5}` → `null` (table) | any `v` is accepted |
| `reconcileSecrets` on a kept blob with `v: 3` → still `v: 3` | `ownBlob` returns `{c,iv}` only |

`onboarding-chat-credentials.spec.ts` — construct the service exactly as
`onboarding-chat-caps.spec.ts:171-176` does. **The input RTU carries no
`credentialsSet` field**, because `reconcileSecrets(…, { deriveCredentialsSet:
false })` leaves an input flag alone and a `true` in the fixture would make the
unconfigured case pass vacuously.

| Assertion | Reddens when |
| --- | --- |
| `KEY=A, VERSION=2`: `mergeDraft(…, { rtuIndex: 0, credentials: { password: "x" } })` → `_secrets.R1.v === 2`, and `rtus[0].credentialsSet === true` (two `it()`s; the second is the **positive control** for the next row) | `enc.keyVersion` is not forwarded |
| key unset: same call → `credentialsSet !== true` and `_secrets` undefined | the deleted branch is restored |

`onboarding-commit.service.rls.integration.spec.ts` — three RTUs: one blob
`{c,iv,v:2}` under A with `KEY=A, VERSION=2`; one blob `{c,iv}` under P with
`PREVIOUS=P`; one with no credential.

| Assertion | Reddens when |
| --- | --- |
| RTU 1's row has `key_version = 2` | the literal `1` survives — the column default is 1, so 2 is the distinguishing value |
| RTU 3's row (no ciphertext) has `key_version = 2` | the null branch writes 1 or the payload's version |
| RTU 2's row has `key_version = 1` **and** `decrypt(c, iv, 1)` round-trips | a version-less blob is labelled with the current version. **`1` alone cannot distinguish "read as 1" from "column default 1" — the unit row above is what kills the `undefined` mutation. Say so in the docblock.** |

## 8. Task 4 — channel secrets decrypt at their stored version (decisions 3, 4)

**Files.** `channels.service.ts` (`loadForRule` select ~857, `toChannelRow` param
~882, decrypt ~914); `channel-reads.ts:53` (its select feeds
`StoredChannelRow = Parameters<toChannelRow>[0]`, so it fails to compile until it
selects the column); `channels.service.spec.ts` (~308 literal).

**Changes.** Both selects add `secretKeyVersion`; the row type gains
`number | null`; the call becomes
`this.crypto.decrypt(row.secretCiphertext, row.secretIv, row.secretKeyVersion)`.
A row with ciphertext and a null version reaches `decrypt`, which refuses it, and
the existing `catch` turns that into `unreadable` with a warn naming the error
class only. **Do not add a `?? 1`.**

| Assertion | Reddens when |
| --- | --- |
| ciphertext + `secretKeyVersion: 4` → `secretState === "ready"`, recorded version `4` | the third argument is omitted or pinned |
| ciphertext + `null` → the recorded version is `null` (decrypt **is** called) | a `?? 1` is added at the call site |
| fake `decrypt` throws with `name === "CredentialKeyVersionError"` → `unreadable`, `secret === null` | the catch narrows to a different class |

## 9. Task 5 — the ingest chain (decisions 2, 3, 4, 5, 9)

**Files.** `bindings.ts` (`BindingRow` 40–60, `BINDING_QUERY` 119–130,
`CredentialDecryptor` 176, `MqttConnectionResolver` 186, `PlanResult` ~90, the
MQTT branch 411–431, the non-MQTT branch 446–462, the result ~577);
`bindings.spec.ts`; `bindings.integration.spec.ts`; `rtu-config.js`; new
`rtu-config.spec.ts`/`.test.ts`; **delete** `rtu-config.test.js` and the inline
`runRtuConfigTests`/`assert` (**ruling 4**); `host/config.ts` and its spec;
`main.ts`; the `tsconfig.build.json` comment.

**Changes.** `BindingRow.key_version: number | null` (null only when the RTU has
no config row — the column is NOT NULL). `BINDING_QUERY` selects
`c.key_version`. `CredentialDecryptor` takes a third `keyVersion` parameter.
`MqttConnectionResolver`'s row gains `key_version` and its return gains
`credentialSource: "db" | "env"`. New
`PlanWarning = { rtuId; rtuCode; reason: "credential-env-fallback" }` and
`PlanResult.warnings`. Both `catch` blocks set `detail:
"key-version-not-loaded"` when `err.name === "CredentialKeyVersionError"` — a
constant, never the message, so §9.6 stays uniform.

`rtu-config.js` imports the resolver from `@bms/shared/credential-keys`;
`isCredentialKeyConfigured()` and `decryptCredentials(ciphertext, iv,
keyVersion)` go through it; **the `createDecipheriv` call stays** — decision 2:
the cipher may stay duplicated, the key selection may not. Everything is
JSDoc-typed so the `main.ts` forwarding is arity-checked. `credentialSource` is
`"db"` only when a username or password came from the decrypted blob; `source`
keeps meaning "a config row existed" and the docblock now says exactly that
(decision 9).

`config.ts` gains `credentialKeyConfigured` and calls the resolver, so a
`CredentialKeyConfigError` propagates as the ingest's refused boot (decision 5).
`main.ts` logs each `initial.warnings` entry once and fixes the "unmodified
pilot file" comment.

**Tests first.** `bindings.spec.ts`: the SQL-text assertion gains
`c.key_version`; a non-MQTT row with `key_version: 7` reaches the recording
decryptor as `7`; an MQTT row reaches the recording resolver as `7`; a resolver
returning `credentialSource: "env"` for a row with a config produces one
warning; returning `"db"`, and a row with no config, each produce none (**both
absence checks, with the row above as their positive control, in its own
`it()`**); a thrown `CredentialKeyVersionError` produces `detail:
"key-version-not-loaded"` while a plain `Error` produces `undefined` (positive
control).

`bindings.integration.spec.ts`: `key_version` is present on every returned row,
`null` for a config-less RTU and a `number` for a planted row — **the only gate
that proves the column exists and arrives as an integer, not text.**

`rtu-config.spec.ts` (a 6-line `encrypt` helper, keys from `randomBytes(32)`,
env saved and restored per function): the moved env-fallback checks; a v2
round-trip; a previous-key round-trip (positive control); **the same ciphertext
labelled `1` throws `CredentialKeyVersionError` — the guess refusal in the
ingest, which a two-key guess would pass**; a current-key ciphertext labelled
`2` throws a non-version error; a row with ciphertext at v2 gives
`credentialSource: "db"` and `source: "db"`; a row with a config but null
ciphertext gives `credentialSource: "env"` and `source: "db"` — **decision 9's
exact narrowing**; a row with ciphertext but no key gives `"env"`; `key_version:
9` throws.

`config.spec.ts`: the version-0 window throws at `readHostConfig`; the same with
`VERSION=2` gives `credentialKeyConfigured === true` (positive control); key
unset gives `false`; a 16-byte previous key throws.

## 10. Task 6 — `rotate-credentials` (decisions 6, 11)

**Files.** New `credential-rotation.service.ts`, `credential-rotation.module.ts`,
`rotate-credentials.cli.ts`, `credential-rotation.integration.spec.ts`/`.test.ts`;
one line in `fleet-read-wiring.spec.ts`; a `scripts` entry in
`apps/api/package.json`.

```ts
export type RotationTableCounts = { scanned: number; rotated: number; skipped: number; raced: number; failed: number };
export type RotationFailure = { table: "rtu_connection_configs" | "notification_channels"; id: string; storedVersion: number | null; error: string /* err.name only */ };
export type RotationReport = { currentVersion: number; rtuConnectionConfigs: RotationTableCounts; notificationChannels: RotationTableCounts; failures: RotationFailure[] };

@Injectable()
export class CredentialRotationService {
  constructor(@Inject(FLEET_DRIZZLE) private readonly fleetDb: BmsDb, private readonly crypto: CredentialCryptoService) {}
  run(db: BmsTx | BmsDb = this.fleetDb): Promise<RotationReport>;
}
```

The `@Inject(FLEET_DRIZZLE)` site carries the §4.3 named reason quoted from the
ADR: rotation is fleet-wide, and an RLS-bound connection would skip every other
organization's rows and still report success.

**The walk selects on ciphertext, never on version** —
`where credentials_ciphertext is not null` / `where secret_ciphertext is not
null`. Per row: `scanned++`; already at the current version → `skipped++`; else
decrypt at the stored version, re-encrypt, and update with a **compare-and-set**
clause (`where id = $id and ciphertext = $old and version = $oldVersion`,
`isNull` for a null channel version) — `rowCount === 1` → `rotated++`, else
`raced++`; any throw → `failed++` with `err.name` only, and continue.
`updated_at` is **not** touched (**ruling 3**). No `JSON.parse` here.

The CLI imports `../load-env`, creates an application context, writes the report
with `process.stdout.write`, sets `process.exitCode` from `failures.length`, and
then closes the app **and ends the three pools** — `DatabaseModule`'s pools have
no `onModuleDestroy`, so the process would otherwise hang.
`CredentialCryptoService`'s constructor is what refuses a dead window before any
row is read.

**Tests first** — `requireIntegrationDb`, a local `withRollback`, fixtures inside
the transaction, env `KEY=B, PREVIOUS=A, VERSION=2` set after the fixtures are
encrypted under A at version 1:

| Assertion | Reddens when |
| --- | --- |
| config row under A/v1 → `{scanned 1, rotated 1, skipped 0, raced 0, failed 0}`, row reads back `key_version = 2` with **different ciphertext bytes** (positive control that something was written) | the walk selects on version, or relabels without re-encrypting |
| that row decrypts under `KEY=B, VERSION=2` with **no** previous key, round-tripping the original plaintext | the rotation wrote the old ciphertext with a new label |
| a config row with NULL ciphertext and `key_version 1` is untouched and not scanned | the walk is version-driven and hands `null` to `decrypt` — decision 6's stated hazard |
| channel under A/v1 → `rotated 1`; no-secret channel not scanned; channel at B/v2 → `skipped 1` | the current-version skip is missing |
| second `run()` → `rotated 0`, both `skipped` | idempotence |
| a row labelled `key_version 7` → `failed 1`, failure `{storedVersion 7, error "CredentialKeyVersionError"}`, its bytes unchanged while the v1 row in the same run changed | a failure aborts the walk, or an unknown version is guessed |
| `encrypt` wrapped to first update the same row (bytes `X`) → `raced 1`, the row holds `X` | the update has no compare-and-set clause and overwrites the concurrent write (**ruling 7**) |
| `injectedToken(CredentialRotationService, 0) === FLEET_DRIZZLE` | the token is swapped — **every other proof uses an explicit pool, so only this line gates the `bms_fleet` claim** |

**Not gated, by construction (§4.6):** the CLI entry and module are wiring like
`main.ts`; the running-container run in §12 is their check.

## 11. Task 7 — configuration and documentation

`.env.example` gains the two variables with a two-line comment.
`docker-compose.yml` gains them on `api` **and on `api-replica`** (**ruling
6** — a replica that cannot read channel secrets answers `unreadable` on every
webhook), and the comment block at 155–163 is rewritten because its fail-open
sentence is now false. The `ingest` service uses `env_file: .env` and needs
nothing. `docs/env-inventory.md` gains two rows in each of the API and ingest
tables. `docs/security/encryption-at-rest.md` §3 replaces "Key rotation is not
implemented" with the runbook — set all three variables on **both** services,
`up -d api ingest`, `exec api pnpm rotate-credentials`, read the counts, unset
`_PREVIOUS` only after a run reports zero rows below the current version, and
finish one rotation before starting the next. `docs/ingest-host.md:46,56` drop
"unmodified".

## 12. Owner rulings

Seven questions, asked at the plan gate on **2026-09-10**, and **all seven ruled
as recommended** by the repository owner. Recorded here rather than folded away,
because two of them widen an accepted ADR and one of them is a plan addition
rather than an ADR decision.

| # | Question | Ruling |
| --- | --- | --- |
| 1 | A malformed **current** key at startup. Today it reads as "unconfigured" and throws only on use; ADR decision 5 names only the previous key. | **Refuse boot.** Dead configuration, the same argument decision 5 already makes. Recorded as **ADR 0062 Amendment 1**. |
| 2 | `CREDENTIAL_ENCRYPTION_KEY_PREVIOUS` set while `CREDENTIAL_ENCRYPTION_KEY` is unset. | **Refuse boot.** The two names were most likely swapped, and a process that can decrypt but not encrypt is a half-done rotation. Recorded as **ADR 0062 Amendment 1**. |
| 3 | Rotation leaves `updated_at` untouched. | **Yes.** F3.50 dates `skipped_unconfigured` deliveries against `max(channel.updatedAt, PROCESS_STARTED_AT)`; bumping it on rotation would count as an operator release and re-open every blocked event key. |
| 4 | Move `rtu-config.js`'s inline checks to a real spec and delete `rtu-config.test.js`. | **Yes.** The carve-out's reason — an unmodified pilot file — ends with this ADR, and a TypeScript spec is type-checked against the new JSDoc signatures. |
| 5 | `reconcileSecrets` with no key still trusts an input `credentialsSet: true` (`onboarding-redaction.ts:436`). | **Leave it**, and record it as a residual in the closure note. Decision 8 removes only the branch that *sets* the flag; changing this is the unconfigured-path change E8.3 deliberately declined. |
| 6 | `api-replica` has no `CREDENTIAL_ENCRYPTION_KEY` at all. Pre-existing. | **Add all three variables** in Task 7. |
| 7 | The compare-and-set clause on the rotation update — a plan addition for decision 6's correctness under a concurrent secret write, not an ADR decision. | **Keep it**, with its test. |

## 13. Corrections the build made to this plan

Recorded rather than rewritten away — the record of the correction is worth more
than a tidy plan. Each was verified against the source, not taken on the
builder's word.

**Task 1 (`9015d71a`).**

1. **§5's row `PREVIOUS=beta alone → error | ruling 2 is dropped` was false as
   written, and would have passed with ruling 2 deleted.** The resolver's order
   puts the version-0 check before the current-key check, and `PREVIOUS=beta`
   alone leaves `VERSION` unset, so `currentVersion === 1` and the *version 0*
   refusal fires first. The row would have gone green against a build with
   Amendment 1's second refusal missing entirely. The fixture is now
   `PREVIOUS=beta, VERSION=2, KEY unset`, and the assertion additionally proves
   the message does **not** contain `version 0` — that negative is what shows
   which of the two refusals was reached. The check order itself was left alone:
   both messages are correct for the state, and reordering to make a test easier
   would have been the wrong repair.

2. **The `keyForVersion(…, NaN)` row cannot redden on any single-guard
   mutation, and is a fail-closed proof rather than a gate.** Dropping
   `Number.isSafeInteger` from the first guard leaves `typeof storedVersion !==
   "number"`, which still catches `null` and `"3"`; `NaN` then falls through
   both comparisons — every `===` against a number is false — and reaches the
   same final `CredentialKeyVersionError` with the same `storedVersion`. No
   input distinguishes the two implementations. **Twelve of thirteen mutations
   reddened; this is the one that did not, and the builder said so rather than
   reporting 13.** The row stays, because what it proves is that NaN cannot
   reach a key, and its docblock now says that is what it proves.

3. Row 13's `message contains "1" and "3, 2"` was weak. The spec pins the whole
   string — `credential key version 1 is not loaded (loaded: 3, 2)` — which also
   fixes the format Task 2's equivalent assertion has to match.

**A fourth, found while checking correction 2 rather than reported.**
`Number.isSafeInteger` in `keyForVersion`'s first guard is **behaviourally
redundant today and no test can gate it**: `resolveCredentialKeys` already
guarantees `currentVersion` is a safe integer, so a non-integer number can equal
neither `currentVersion` nor `currentVersion - 1`, and every path ends at the
same throw. It stays as a statement of intent and as the guard that survives if
`currentVersion` ever stops being parsed from `/^\d+$/`. Do not delete it to
chase coverage, and do not write a test claiming to cover it.

**Two design notes from the build, neither a defect.**
`loadedKeyVersions({ currentVersion: 1, current: null, previous: null })`
returns `[1]`; the plan does not address an unconfigured current slot, and
`keyForVersion` reaches the `is required` refusal first for version 1. And a
decoder that *throws* — an `atob`-style one — is not wrapped into
`CredentialKeyConfigError`; both call sites inject `Buffer.from`, which never
throws, so the case is unreachable from this repository.

**Task 2 (`3462ec73`).**

4. **§6's row `KEY=A, PREVIOUS=<16 bytes> → constructor throws` would have
   passed against a build with the length rule deleted — the same defect as
   correction 1, one task later.** `decodeKey` enforces the 32-byte length
   *before* `resolveCredentialKeys` reaches the version-0 check, so deleting the
   length rule lets the code fall through to the version-0 refusal, which throws
   the same error class, and the row goes green. Fixed with `VERSION=2` and a
   pair of assertions: the message contains `32 bytes` **and** does not contain
   `version 0`.

   **This is now a pattern in this plan, not an accident.** Every one of these
   refusals lives in one function and throws one error class, so a fixture that
   leaves an earlier guard live cannot gate a later one — the test sees a throw
   and cannot tell which guard produced it. Any remaining task that asserts on a
   `CredentialKeyConfigError` must pin *which* refusal was reached, with a
   negative assertion against the neighbouring message. Tasks 5 and 6 both have
   rows of this shape.

5. **The plan's mandated fixture shape does not, by itself, gate the per-call
   resolver read.** §6 requires each row to construct the service after setting
   the environment, and says that is what catches the read-at-construction
   mutation. It is not: because every row constructs *after* the env is final,
   an implementation that caches the window in its constructor satisfies the
   whole table. The gate is a **second** env change, after construction — the
   round-trip row now re-encrypts on the same instance and asserts the stamp
   moved from 2 to 3. Measured: that mutation reddens on the re-encrypt and on
   nothing else. The builder's own first docblock claimed the `decrypt` above it
   caught the mutation; it did not, and the file now records what was measured
   rather than what was assumed.

6. **`isConfigured()` widened, and the plan did not say so.** It now answers
   `false` for a *dead* window — a previous key at version 1, or a malformed
   version — even when `CREDENTIAL_ENCRYPTION_KEY` itself is a valid 32 bytes;
   the old implementation read that one variable. This is unreachable in a
   booted application, because the constructor refuses the same states first,
   but `readiness.ts:44` reaches the static without constructing anything. The
   widening is documented at the call site rather than in the plan.

Seven mutations were run on Task 2 and **all seven reddened**, including the
try-both-keys one on the label-`1` row. `pnpm typecheck:tests` fails at exactly
one line — `channels.service.ts:914`, `TS2554` — which Task 4 owns; it is
typecheck-red only, since `channels.service.spec.ts:106` fakes `crypto` with
`encrypt` alone and never reaches `decrypt`.

**Task 3 (`8dcab28b`, `cb15fe24`).** Four mutations run, all four red:
`ownBlob` dropping `v` (3 unit tests), `readEncryptedCredentials` defaulting to
`undefined` (1), the commit-site literal `1` restored (the new integration case,
`expected 1 to be 2`), and the deleted `credentialsSet` branch restored (1).

7. **§7's integration fixture was not buildable as described.** A bare
   three-RTU draft never reaches `readyToCommit`: `inferPhase` requires a point
   key, an asset and an asset point before it will report `review`, so the
   commit under test never runs. The fixture now carries one of each, mirroring
   `commitReadyDraft`. The plan named the draft's RTUs and forgot what makes a
   draft committable at all.

8. **The two blobs have to be produced by `CredentialCryptoService.encrypt`
   under two temporarily-loaded windows, not hand-built as JSON.** §7 did not
   say so. It matters: a hand-built blob makes the round-trip assertion a
   restatement of the fixture, while a real encrypt under the old key and a real
   decrypt under the window is the only thing that proves decision 4's selection
   end to end at this layer.

**A reporting imprecision worth keeping, since it is the shape of a false
green.** The build reported `onboarding` as "95/95 passed, both with and without
`DATABASE_URL`". Re-run here: **without** the variable it is 91 passed and
**4 skipped** — the integration cases fail closed and do not run. With
`DATABASE_URL` set they pass, 4 of 4, which is what actually gates correction 7
and the commit-site literal. A future reader must not take the without-variable
number as evidence the database layer was covered.

**Task 4 (`17af0bd0`).** `pnpm typecheck:tests` passes end to end again, all
five legs, confirmed independently. `alarm-lifecycle.service.ts:342` needed no
change once `channel-reads.ts` selected the column.

9. **§8's third row names a discrimination that does not exist.** The plan says
   the row reddens when "the catch narrows to a different class". The `catch` is
   **class-blind** — it catches everything — so that mutation reddens by the
   error *escaping a method contracted never to throw*, not by class handling.
   The row is still worth having; its stated reason was wrong.

10. **The mutation that actually gates the class claim is not in the plan.**
    Changing `err.name` to `err.message` in the warn is what reddens the
    assertion that the log names the class and not the message. Recorded so the
    review does not re-derive it.

11. **No green test proves `secret_key_version` arrives from Postgres as a
    number.** Both integration specs that reach `toChannelRow`
    (`cleared-refusal-rows:115`, `control-character-error:127`) plant channels
    with **no** ciphertext, so the function returns at its null branch before
    the version is read; `storm-control`'s two call sites sit past a failure at
    its line 181. §14's planted channel row is the only gate for that claim, and
    it is a manual one. **Do not let the closure note claim this is covered.**

12. **A false sentence this branch created, fixed here rather than deferred.**
    `channels.service.ts:47-52` still said `CredentialCryptoService.getKey()`
    throws when the key is unset or the wrong length. Task 2 deleted `getKey()`,
    and an unset key now returns `current: null` without throwing, while a
    wrong-length key refuses the boot under Amendment 1 and never reaches this
    service at all. The build left it "for whoever owns that sweep"; it is a code
    docblock in a file this task already edits, not an AGENTS/BACKLOG item, so
    §9.10 does not apply and deferring it would have shipped a false sentence
    made false by this very branch.

**Two facts for whoever closes the row, neither a defect of this task.**
`storm-control.integration` is red on this machine and **reproduces at
`982e8642` with Task 4's four files reverted** — the cause is local database
pollution, `rule_notifications` joining every `F310_*` rule to three leftover
`f360-*` channels from the other session's F3.60 work. It is not this branch's
breakage and must not be "fixed" here. And `channels.service.ts` now stands at
**990 lines against AGENTS.md §4.5's 1000-line cap** — ten left after correction
12's docblock, so the next task to touch it inherits a split.

**Task 5 (`d931e97a`).** Fifteen mutations run, **fifteen red**, each on its
intended row only — including the two run against `packages/shared` to prove
Trap A's negative assertions discriminate: the 32-byte rule and the version-0
guard each reddened only their own `config.spec.ts` row. `packages/shared` was
reverted byte-for-byte afterwards and `dist` rebuilt.

13. **§9's file list does not fit under §4.5.** Adding the new binding
    assertions to `bindings.spec.ts` takes it to **1002 lines** and the
    pre-commit hook blocks the commit. They live in
    `bindings-credentials.spec.ts` with the fixtures exported and shared — the
    `supervisor-buffer.spec.ts` ↔ `supervisor.spec.ts` precedent, and
    `disk-buffer-modes.spec.ts` exists for the same reason. **The plan should
    have counted the lines before naming the file.**

14. **§9 asks for one `detail` assertion; two `catch` blocks need four.** One
    version/plain pair per branch: mutation M5a (`err.name` → `err.message`)
    reddens only the two version rows and M5b (detail set unconditionally) only
    the two plain rows, so a single pair would have gated one branch and left
    the other free.

15. **A false sentence this branch created in an ADR it does not own — now
    ADR 0016 Amendment 6.** `docs/adr/0016-ingest-adapter-framework.md:575-580`
    said `rtu-config.js` is "reused with zero logic changes" and keeps its
    export "so `rtu-config.test.js` — the one ingest test CI runs today — keeps
    passing unmodified". Decision 2 changed the file and ruling 4 deleted that
    test. **§15's sweep list did not cover this**: it names AGENTS.md's Ingest
    adapters row, `docs/BACKLOG.md` and `docs/roadmap.md`, and no ADR but 0062.
    The build was right not to edit the historical text — that is human-gated —
    and right to report it; the repair is an amendment, which is the house
    pattern and lands with the feature.

16. **`isCredentialKeyConfigured` now has no caller outside `rtu-config.js`**,
    because `main.ts` reads `hostConfig.credentialKeyConfigured` instead — the
    new field would otherwise be dead. It stays exported as the ADR 0012 seam
    (Amendment 6 point 3) and is gated indirectly: a wrongly-`true` answer
    throws in `runCiphertextWithoutKeyTests`.

**Coverage was not measured for Task 5, and the build did not claim it was.**
`main.ts`'s `initial.warnings` loop is uncovered by construction, like the rest
of that file. A full `test:coverage` run would include the polluted
`storm-control.integration` failure above and produce a number nobody could
interpret. **The closure note must measure coverage on a clean database or say
plainly that it did not.**

**Task 6 (`eea84ad4`).** Thirteen mutations run, **twelve red**; the CLI builds
and was run against the live database in both a working and a dead key window.
Built on **Fable**, which the closure row should record — the commit trailer was
normalised to the session's attribution line to match the branch's other six
commits, since this repository keeps per-unit model attribution in the backlog
row rather than in git trailers.

17. **"Fixtures inside the transaction" is not enough for a fleet-wide walk,
    and §10 said it was.** The walk reads every row in both tables, so rows
    *committed by other suites* are scanned too. This machine held three
    `rtu_connection_configs` rows — two of them with ciphertext — that a killed
    `onboarding-commit.rls` run left behind at `2026-09-10 18:35:22`; every
    absolute count in the spec was off by two and `failures` carried foreign
    ids. The spec now runs at `repeatable read` and nulls committed secrets
    inside its transaction before planting. **Residual, stated rather than
    hidden:** a concurrent delete inside the snapshot-to-quarantine window
    surfaces as a named `40001`, not as a wrong count.

    Those rows were **this plan's own leavings, not another session's** — RTU
    codes `E71B-OBK-RTU-{A,P,N}-1789065321887`, from Task 3's integration spec.
    Deleted on 2026-09-11 along with the location, point key, asset and asset
    point of the same run; both credential tables read 0 again and `bms.rtus` is
    back to its 56 seeded rows. The 62 `FIXTURE-F3*` assets in the same window
    belong to the other session's F3.60 work and were left alone.

18. **The `isNull` compare-and-set branch is unreachable twice over.**
    `notification_channels_secret_complete_check` (migration 0038) forbids
    ciphertext without a version, so the row cannot be planted at all, and
    `decrypt` refuses `null` before any update would run. It is kept for type
    totality and its docblock says no test reaches it.
    `RotationFailure.storedVersion: null` is unreachable for the same reason.
    **This also means Task 4's null-version row exercises a state the database
    forbids** — correct as a defensive unit assertion, and worth knowing before
    anyone calls it live coverage.

19. **Ruling 3 had no row in §10's table.** `updated_at` was ungated by the plan
    as written; a ninth case now gates it and mutation M3 reddens only there.

20. **Only the bytes half of the compare-and-set is gateable.** §10 wrote
    "version and bytes" as one clause. Dropping the version clause while keeping
    the bytes clause **did not redden** (M2c) — any genuine re-encryption
    changes the bytes, so the bytes clause subsumes it. The race fixture writes
    new bytes at the *old* version, which is what a version-only check would
    have overwritten.

21. **"One line" in `fleet-read-wiring.spec.ts` is three** — the assertion, its
    import, and a docblock clause that would otherwise stop being true.

22. **A wrong-key row reports `error: "Error"`.** An AES-GCM tag failure throws
    a bare `Error`, so the rotation report cannot distinguish "wrong key" from
    any other decrypt failure, while an unloaded version reports
    `CredentialKeyVersionError` precisely. A named class in
    `credential-crypto.service.ts` would fix it. **Not done here** — that is
    Task 2's file and this is a report-quality improvement, not a decision of
    ADR 0062. It belongs in the closure note as a follow-up row.

**Task 7 (`4c4c2091`, `bc60d0f0`).** Docs and compose only; `typecheck:tests`
and the repo suite did not move, which is what a documentation change should do.
The second commit exists because a review of the first found three more
sentences this branch had made false — §11 named five files and the branch had
falsified prose in more of them than the plan listed, for the third time
(see corrections 12 and 15).

23. **The biggest finding of Task 7 is not a documentation defect.** §11 asked
    for a runbook; writing it truthfully exposed **a third ciphertext carrier
    the rotation command does not walk**. An onboarding draft's `_secrets`
    store holds ciphertext with its own version — Task 3 put it there — but it
    lives in `bms.onboarding_sessions.draft`, and decision 6's walk reads two
    tables. Commit copies the draft's version verbatim rather than
    re-encrypting, which is correct (relabelling under a key that did not write
    the bytes is what decision 4 refuses), so **a draft started before a
    rotation and committed after it lands at the old version once the walk has
    already reported clean.** Unsetting the previous key then makes it
    unreadable.

    Verified rather than reasoned: `credential-rotation.service.ts` imports
    only `rtuConnectionConfigs` and `notificationChannels`;
    `onboarding-commit.service.ts:334-344` assigns `keyVersion =
    enc.keyVersion`; and `bms.onboarding_sessions` holds **0 drafts with a
    `_secrets` entry** as of 2026-09-11, so nothing is exposed today.

    Handled operationally in the runbook (§3.1 step 5: drain or discard open
    drafts before unsetting the previous key) and recorded in ADR 0062's
    Consequences. **Extending the command to rewrite draft blobs is a follow-up
    row.**

24. **`docs/ingest-host.md`'s env table still lists only
    `CREDENTIAL_ENCRYPTION_KEY` for the ingest**, not the two new variables. The
    task's file list named lines 46 and 56 only, so it was left alone and
    reported. Owed to the sweep or to whoever next touches that table.

**Step 5 — the review gates (2026-09-11).** `security-reviewer` and
`agents-compliance-reviewer` both returned. No Critical, no High, no scope
excess, no dependency block touched, and both confirmed the guess refusal is a
refusal and that no key material or plaintext reaches a log, an error or the
rotation report. Between them they found one behavioural hole and six false or
incomplete sentences, all fixed on the branch.

25. **Decision 9's ingest half could not fire in the ordinary sequence.**
    `main.ts` read `initial.warnings` only, so an RTU that entered the
    credential fallback at a *reload* — which is what happens when an operator
    onboards an RTU into a running host, the ordinary case — was never named at
    all. The reload kept `skipped` and dropped `warnings`. Worse, the comment
    above the loop claimed "**once per RTU per process**", so the code
    contradicted its own docblock in the same file. Fixed with a per-process
    `Set` of warned RTU ids, reported from both the initial plan and every
    reload: an unchanged fallback still is not repeated each cycle, and a new
    one is named the first time the process sees it.

    **This fix is in `main.ts`, which has no test.** The plan already records
    that file as uncovered wiring; the change is a `Set` and a loop, verified by
    `typecheck:tests` and by reading, not by a gate. Said plainly rather than
    implied.

26. **Four more false sentences, in the branch's own documents.** That is the
    fourth, fifth, sixth and seventh of this branch — see corrections 12, 15 and
    23 for the earlier ones.
    - `encryption-at-rest.md` said the fallback is logged "on every reload".
      `main.ts` said the opposite in its own comment. Now: once per RTU per
      process, at boot or at the reload that first serves it.
    - The same file said the draft "reports `credentialsSet: false`" with no
      key. Owner ruling 5 accepted a residual: `reconcileSecrets` runs with
      `deriveCredentialsSet: false`, so a client- or model-asserted `true`
      **survives**. The document now states the residual instead of the
      stronger claim.
    - It cited `onboarding-commit.service.ts:166-176` for the null-ciphertext
      write. That range is an unrelated `F4.103` comment; the site is `:332-352`.
    - `credential-crypto.service.ts:74` cited `channels.service.spec.ts:247-292`
      for specs that mutate `process.env` after construction. The behaviour is
      real; the range was stale. Now `:175-191` and `:335-380`, verified.

27. **`.env.example` named one of two boot refusals.** It said the previous key
    must be empty unless the current key is set, and never mentioned that a
    previous key with `CREDENTIAL_ENCRYPTION_KEY_VERSION` empty or `1` also
    refuses the boot — which is exactly what the shipped file's own empty
    `VERSION=` produces. An operator following it literally could not start.

28. **The runbook restarted `api` and `ingest` but not `api-replica`** — owner
    ruling 6 followed in letter and not in substance, since that ruling exists
    precisely to give the replica the keys. The failure is silent, not loud: the
    replica's pre-rotation window is valid, so no boot refusal fires; after the
    walk every channel secret is at version *N+1*, `keyForVersion` refuses it,
    and `WebhookTransport` records `skipped_unconfigured` while sending nothing.

29. **`raced` appeared in the report and nowhere in the runbook.** An operator
    reading a non-zero `raced` needs to know it is neither an error nor a
    completion — the row is untouched and still at its old version, and the
    command must be run again before step 5's test is met.

30. **Reported and deferred, a decision gap rather than a defect:**
    `rtu-config.js` sets `credentialSource: "db"` when *either* the username or
    the password came from the decrypted row. A row holding only a username
    leaves the password on the global account, reports `"db"`, and raises no
    warning. The code matches its docblock and decision 9; closing it needs a
    per-field source or a broader warning, which is a follow-up row.

**`code-reviewer` returned last**, and independently found corrections 25 and 28
while they were being repaired. It added one Correctness finding neither other
gate saw, and it cleared the branch of the failure mode this plan has been most
worried about.

31. **The rotation command prints the runbook's completion signature and exits
    0 when the process never picked up the new window.** `run()` takes
    `currentVersion` from `CredentialCryptoService.currentKeyVersion()` — the
    version *this process* holds. Edit the compose env at step 1, forget
    `docker compose up -d` at step 2, and the container still holds version 1:
    every row reads 1, every row is `skipped`, `failures` is `[]`, exit 0. Step
    5's completion test — `skipped == scanned`, `failures: []` — is satisfied
    **bit for bit**, and the operator then unsets the previous key against rows
    nothing has touched. This is exactly what decision 6 warned of: *a rotation
    that silently accepts an unknown version can never report that it has
    finished.*

    Fixed twice, because neither half is sufficient alone. The CLI now refuses
    outright when `isConfigured()` is false — a rotation with no current key
    cannot re-encrypt anything. And step 4 of the runbook now opens by requiring
    the operator to check `currentVersion` in the report against the version
    they set, because a **stale** version is indistinguishable from a finished
    rotation *from inside the command*: it cannot know what the operator
    intended. The report already carried the discriminating field and nothing
    told anyone to read it.

32. **The one file whose subject is "which guard answered" miscounted its own
    subject.** `config.spec.ts`'s docblock said "every one of the **four**
    functions below asserts on the same error class" (two of them assert a
    *successful* boot and never see an error), said "**four** different guards"
    and then listed five, and claimed the negative-assertion rule held for all
    of them. All three were false. The two acceptance functions are now named
    for what they are — the positive controls without which a
    `resolveCredentialKeys` that threw on everything would satisfy every other
    function in the file.

33. **"The ingest log names every RTU still sharing the global broker account"
    was false, and backwards.** With no key, `POST :id/credentials` answers 503
    before anything is stored, so no `rtu_connection_configs` row exists, so
    `configRow` is `null` and the warning is guarded off. The count is **zero,
    not partial** — and `bindings-credentials.spec.ts:155-161` gates that
    absence deliberately. The paragraph now says so, and says what the warning's
    real subject is: a row that exists and holds no readable credential.

34. **The round-trip integration assertion decrypted at a literal `1`** rather
    than at the version read back out of the column, which made it restate the
    assertion above it instead of depending on it. It now reads
    `previousRow.key_version`.

35. **Correction 25's repair ships with no unit gate**, because `main.ts` has no
    test — the same condition that let the original defect through. Said in the
    code rather than implied, and §14 gains **ingest step 5** as the layer that
    stands in: plant a credential-less config row against a *running* host, see
    the line appear at the next reload, and see it **not** appear at the one
    after. Both halves are the assertion; either alone passes against a build
    this replaced.

**The result the plan cared about most.** `code-reviewer` walked every
assertion resting on `CredentialKeyConfigError` — eleven rows across three spec
files — and found **no remaining instance** of the shape corrections 1, 4 and 32
describe: each fixture either reaches the first guard that can fire for its
input, or carries the negative assertion naming which refusal answered. It also
confirmed every exported assertion has a caller and that no coverage threshold
was lowered. **No false green.**

## 14. Verification — which layers, and why the N/A ones are N/A

> **Status 2026-09-11: the container layer below has NOT been run, by owner
> decision, and `E8.4` cannot close until it is.** Every cheap gate is green —
> `typecheck:tests` across all five legs, `shared` + `ingest` 132 passed / 5
> skipped, `api` with `DATABASE_URL` 128 passed / **0 skipped**, and 63
> mutations run per task of which 61 reddened, the two exceptions recorded in
> §13 with their reasons. What is missing is the live stack.
>
> The reason is not cost. `docker compose up -d api api-replica ingest`
> recreates containers **another session is actively using for `F3.60`**, and
> would leave that session's stack serving this branch's code until they
> rebuilt — silently invalidating any measurement they had already taken. The
> owner ruled to wait rather than to disrupt them or to skip the layer.
>
> **Ingest step 5 is the one that must not be dropped when this is picked up.**
> It is the only gate on correction 25's repair, because `main.ts` has no unit
> test and the code says so in a comment rather than implying otherwise.

Build the images from **this worktree** as context, with absolute `-f` and
context paths and `--no-cache` — a worktree build has served `main`'s code
before. `docker compose build` restarts nothing; `up -d api ingest` does.
Confirm the container's `Created` time is after the last commit and that
`exec api ls dist/security/` lists `rotate-credentials.cli.js` **before** reading
anything from it.

**Database — applicable.** Rows are rewritten; no schema changes. Count and read
as `bms_fleet`, never `bms_owner` — FORCE RLS returns 0 rows to the owner.
Re-measure both tables first (0 rows as of 2026-09-10). Plant one
`rtu_connection_configs` row on a seeded ingest-enabled PHE RTU whose
credentials equal the env `MQTT_USERNAME`/`MQTT_PASSWORD`, so the broker
connection is unchanged, with ciphertext produced **inside the container** (the
key is set there and unset in the shell) at `key_version = 1`; plant one webhook
`notification_channels` row the same way. After rotation both read version 2
with changed bytes. **Delete both planted rows at the end** — `compose up`
re-seeds and verifies counts, and a leftover row alters what the ingest connects
with.

**API — applicable.** (1) Negative boot: a previous key with no version → the
container exits and the log names `CredentialKeyConfigError` and version 0.
(2) Positive boot with `VERSION=2` → `/health` is 200. (3)
`exec api pnpm rotate-credentials` → `rotated: 1` per table, `failures: []`,
exit 0; a second run → `skipped: 1` each. (4) Set the config row to
`key_version = 7` and run again → `failed: 1`, `error:
"CredentialKeyVersionError"`, exit 1, row unchanged. All `curl`, `docker compose
exec` and `psql` — no browser claim.

**Ingest — applicable.** (1) The same negative boot, via `ingest host failed to
start`. (2) With the window set and the planted row at v1 then v2: no
`rtu skipped` and no `rtu credential fallback` line. (3) `key_version = 9` →
`rtu skipped … reason=credential-decrypt-failed detail=key-version-not-loaded`.
(4) `credentials_ciphertext = NULL` → `rtu credential fallback …
reason=credential-env-fallback`. **Read the log lines; do not infer them.**

**(5) The reload path — added after the 2026-09-11 security review, and the only
gate on correction 25, because `main.ts` has no unit test.** With the host
already running and its log quiet, plant an `rtu_connection_configs` row holding
a config but no readable credential, and wait one `INGEST_RELOAD_MS` cycle
**without restarting anything**. The `rtu credential fallback` line must appear
for that RTU. Then wait a second cycle: it must **not** appear again. **Both
halves are the assertion.** The first alone passes against a build that logs on
every reload; the second alone passes against the boot-only build this
replaced — which is precisely the defect the review found.

**Browser — N/A, and here is why.** No file under `apps/web` changes. The one
user-visible behaviour change — decision 8, `credentialsSet` staying false when
the key is unset — is unreachable through the API today, because
`POST :id/credentials` answers 503 before `mergeDraft` runs, held by
`onboarding-credentials.spec.ts`'s fail-closed assertion; and the branch itself
is held by `onboarding-chat-credentials.spec.ts`. Nothing here needs
`browser-verifier`.

Check both directions wherever a guard exists: the boot refusal must fire on the
bad window **and not** on the good one; the fallback warning must fire on a
ciphertext-less row **and not** when the row decrypts.

## 15. Commit shape and the gates still owed

Seven feature commits in the order above, each with green unit tests and
`pnpm typecheck:tests` before the next task starts. PR title
`feat(E8.4): credential key rotation and unconfigured-key visibility (ADR 0062)`.

**Not in this branch:** the `chore(agents):` sweep — AGENTS.md's Secrets row
gains the two-key window; the Ingest adapters row loses "unmodified
`rtu-config.js`" and gains decision 9; `docs/BACKLOG.md` and `docs/roadmap.md`
record that decisions 1–9 shipped and decision 10 stays open. A separate PR
after merge, per §9.10 and ADR 0062's Consequences.

Gates: step 2 (ADR accepted) **passed**; step 5 `code-reviewer`,
`security-reviewer`, `agents-compliance-reviewer` — no `migration-reviewer`,
since nothing under `packages/db` changes; step 7 merge approval **owed to the
owner**; the sweep PR **owed** after merge, with `E8.4` staying open on
decision 10.
